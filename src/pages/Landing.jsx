import { useState, useEffect } from 'react';
import { Link, useSearchParams } from 'react-router-dom';
import { pagesAPI, contactsAPI } from '../services/api';
import useWindowSize from '../hooks/useWindowSize';
import EditableSection from '../components/EditableSection';
import arrowImage from '../assets/Arrow 1.png';
import arrow2Image from '../assets/Arrow 2.png';
import arrow12Image from '../assets/Arrow 12.png';
import quoteIcon from '../assets/Screenshot 2026-02-20 095834.png';
import oliveBranchLeft from '../assets/olive-branches-award-symbol 3.png';
import oliveBranchRight from '../assets/olive-branches-award-symbol 5.png';

const Landing = () => {
  const { isMobile, isTablet } = useWindowSize();
  const [searchParams] = useSearchParams();
  const isEditorMode = searchParams.get('editor') === 'true';
  const [selectedSection, setSelectedSection] = useState(null);
  const [openFaq, setOpenFaq] = useState(null);
  const [countdown, setCountdown] = useState({ days: 0, hours: 0, mins: 0, secs: 0 });
  const [currentTestimonialIndex, setCurrentTestimonialIndex] = useState(0);

  // Form state
  const [formData, setFormData] = useState({
    fullName: '',
    email: '',
    companyName: '',
    phone: '',
    message: '',
  });

  // Default page content
  const [pageContent, setPageContent] = useState({
    // Top Banner
    bannerText: 'Egestas facilisi commodo in sapien integer ut',
    bannerEndDate: '2026-03-01 00:00',
    showBanner: 'yes',
    // Hero
    heroTitle: "We Build Shopify Websites That Don't Just Look Good —",
    heroTitleHighlight: 'They Sell.',
    heroDescription: "India's trusted Shopify agency delivering design, development, and conversion results you can count on.",
    heroButtonText: 'Get A Web Design Quote',
    heroButtonLink: '/home',
    heroImage: '',
    heroMedia: '', // New field for image or video
    stat1Value: '2-3 weeks',
    stat1Label: 'Average delivery',
    stat2Value: '$2M+',
    stat2Label: 'Revenue generated',
    stat3Value: '100%',
    stat3Label: 'Satisfaction rate',
    // Trusted By
    trustedByTitle: 'Trusted by leading brands and recognized by',
    badge1Text: 'Shopify Plus Partner',
    badge2Text: 'Featured on Forbes',
    badge3Text: 'Inc. 5000',
    badge4Text: 'Best Design Award',
    badge5Text: 'E-commerce Expert',
    review1Rating: '4.9',
    review1Source: 'on Google (127 reviews)',
    review2Rating: '4.9',
    review2Source: 'on Trustpilot (127 reviews)',
    review3Rating: '4.9',
    review3Source: 'on Clutch (127 reviews)',
    // Portfolio
    portfolioImages: [],
    portfolioCategory: 'Fitness and Wellness',
    // Stats
    statsTitle: 'Real Numbers, Real Impact',
    statLive1Value: '$280,366',
    statLive1Label: 'Total Revenue Generated by our clients',
    statLive2Value: '261',
    statLive2Label: 'Stores Launched and counting',
    statLive3Value: '5.8%',
    statLive3Label: 'Average Conversion Rate vs 2.5% industry avg',
    statLive4Value: '4.9/5',
    statLive4Label: 'Client Satisfaction from 200+ reviews',
    statLive1Icon: '',
    statLive2Icon: '',
    statLive3Icon: '',
    statLive4Icon: '',
    // Hidden Cost
    hiddenCostTitle: 'The Hidden Cost of Cheap',
    hiddenCostTitleHighlight: 'Shopify Stores',
    painPoint1Title: 'Waiting 3-6 months for your store to launch',
    painPoint1Desc: 'Your competitors are already capturing your market share',
    painPoint1Stat: '73% of customers buy from the first store they find',
    painPoint2Title: 'Freelancers who disappear after the first payment',
    painPoint2Desc: 'Freelancers who disappear after the first payment',
    painPoint2Stat: '$8,500 average loss on failed freelance projects',
    painPoint3Title: 'Paying per revision or getting ghosted when you ask for changes',
    painPoint3Desc: 'Your vision stays in your head, never making it to the screen',
    painPoint3Stat: '67% of clients unhappy with "final" deliverables',
    painPoint4Title: "Beautiful designs that don't actually convert",
    painPoint4Desc: 'High traffic but no sales means burning money on ads',
    painPoint4Stat: 'Average e-commerce conversion rate is only 2.5-3%',
    // Familiar
    familiarTitle: 'it\'s because too many brands settle for "cheap" that ends',
    familiarTitleHighlight: 'up costing more later',
    familiarDescription: 'At GOTI, we do it right from the start — thoughtful design, solid development, and strategies backed by data. We build Shopify stores that earn trust and drive conversions from day one.',
    // Comparison
    col1Title: 'DIY with Templates',
    col1Subtitle: 'Your time is worth more than this',
    col1Items: '6-12 weeks of your time\nGeneric, cookie-cutter design\nNo conversion optimization\nTechnical headaches\nLooks like 1000 other stores\nNo support when issues arise',
    col2Title: 'GOTI Premium',
    col2Subtitle: 'Investment that pays for itself',
    col2Items: '2-3 week launch timeline\nConversion-focused design\nUnlimited revisions\nE-commerce psychology experts\n10-day money-back guarantee\n30 days post-launch support\nlorem ipsum dolae sitliw',
    col3Title: 'Cheap Freelancers',
    col3Subtitle: 'Cheap always costs more in the long run',
    col3Items: '3-6 month timeline\nCommunication nightmares\nQuestionable quality\nRevisions cost extra\nThey disappear after payment\nNo business understanding',
    // Process
    processTitle: 'Our Simple Process',
    processSubtitle: 'From concept to launch in 4 simple steps. No complexity, just results.',
    processImage: '',
    step1Icon: '',
    step1Title: 'Discovery Call',
    step1Desc: 'We start by understanding your brand, goals, and target audience. This 30-minute call sets the foundation for your perfect store.',
    step2Icon: '',
    step2Title: 'Design & Branding',
    step2Desc: 'Our designers create a stunning, conversion-optimized design that reflects your brand and appeals to your customers.',
    step3Icon: '',
    step3Title: 'Development',
    step3Desc: 'We bring the design to life on Shopify, ensuring every element is optimized for performance and conversions.',
    step4Icon: '',
    step4Title: 'Launch & Support',
    step4Desc: 'After your approval, we launch your store and provide ongoing support to ensure everything runs smoothly.',
    processCTAText: 'Claim Your Spot Now',
    // Value
    valueTitle: "Now Let's Talk About",
    valueTitleHighlight: "What You're Getting...",
    feature1: 'Custom Shopify Theme Development',
    feature1Desc: 'Unique design that converts',
    feature2: 'Responsive Mobile Optimization',
    feature2Desc: 'Perfect on every device',
    feature3: 'SEO Setup & Optimization',
    feature3Desc: 'Get found on Google',
    feature4: 'Product Page Templates',
    feature4Desc: '5 high-converting variations',
    feature5: 'Shopping Cart & Checkout Customization',
    feature5Desc: 'Reduce cart abandonment',
    feature6: '30 Days Post-Launch Support',
    feature6Desc: "We've got your back",
    worthText: 'All of these are worth ~ $5000',
    worthSubtext: 'other agencies charges.. $6000-$10000',
    payText: 'But with GOTI, you only pay',
    priceValue: '$1,200',
    priceDiscount: '70% Off',
    priceSubtext: 'One-time investment. Lifetime value.',
    priceCTAText: "Let's Make It Happen",
    guarantee1: '2-3 Week Launch',
    guarantee1Sub: 'Get to market fast',
    guarantee2: 'Unlimited Revisions',
    guarantee2Sub: "Until it's perfect",
    guarantee3: '10-Day Guarantee',
    guarantee3Sub: 'Risk-free promise',
    // Testimonials
    testimonialTitle: "Don't Just Take",
    testimonialTitleHighlight: 'Our Word For It',
    testimonialSubtitle: "Watch real store owners share their success stories. These aren't actors—they're actual clients who've transformed their businesses with GOTI.",
    testimonials: [
      {
        id: 1,
        quote1: 'Lorem ipsum dolor sit amet consectetur. Ullamcorper amet arcu quis elementum. Convallis purus mauris at in.',
        quote2: 'Pretium pharetra aliquam consequat duis ac risus vitae sollicitudin pharetra.',
        authorName: 'Reema Roy',
        authorRole: 'Head of Marketing, Company-name',
        authorImage: '',
        stat1Value: '+45%',
        stat1Label: 'AOV (Average Order Value)',
        stat2Value: '+24%',
        stat2Label: 'CTR (Click-through rate)',
        stat3Value: '+16%',
        stat3Label: 'Return-rate per customer',
      },
    ],
    // Social Proof
    reviewCount: '990+',
    reviewText: 'Join 200+ successful store owners',
    reviewSubtext: 'demo text goes here.',
    moreCount: '+195 more',
    // FAQ
    faqTitle: 'Frequently Asked Question',
    faq1Question: 'Lorem ipsum dolor sit amet consectetur?',
    faq1Answer: 'Lorem ipsum dolor sit amet consectetur. In faucibus aliquam turpis maurstibulum malesuada.',
    faq2Question: 'Lorem ipsum dolor sit amet consectetur?',
    faq2Answer: 'Lorem ipsum dolor sit amet consectetur.',
    faq3Question: 'In faucibus aliquam turpis maurstibulum malesuada.?',
    faq3Answer: 'Lorem ipsum dolor sit amet consectetur.',
    faq4Question: 'Lorem ipsum dolor sit amet consectetur?',
    faq4Answer: 'Lorem ipsum dolor sit amet consectetur.',
    faq5Question: 'Lorem ipsum dolor sit amet consectetur. In faucibus aliquam turpis mauris convallis vestibulum malesuada.?',
    faq5Answer: 'Lorem ipsum dolor sit amet consectetur.',
    faq6Question: 'Lorem ipsum dolor sit amet consectetur?',
    faq6Answer: 'Lorem ipsum dolor sit amet consectetur.',
    faq7Question: 'Lorem ipsum dolor sit amet consectetur?',
    faq7Answer: 'Lorem ipsum dolor sit amet consectetur.',
    // Contact Form
    formTitle: 'Have More Question?',
    formSubtitle: 'Book A Call',
    formTags: 'Branding, Packaging, Website',
    formButtonText: 'Submit Now',
    formResponseText: 'Get Instant Response',
    // Landing Footer
    copyrightText: 'Copyright© 2025 GOTI.DESIGN. All rights reserved.',
    siteUseLink: 'Site Use',
  });

  useEffect(() => {
    fetchPageContent();
  }, []);

  // Countdown timer
  useEffect(() => {
    const endDate = new Date(pageContent.bannerEndDate).getTime();
    const timer = setInterval(() => {
      const now = new Date().getTime();
      const diff = endDate - now;
      if (diff > 0) {
        setCountdown({
          days: Math.floor(diff / (1000 * 60 * 60 * 24)),
          hours: Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60)),
          mins: Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60)),
          secs: Math.floor((diff % (1000 * 60)) / 1000),
        });
      }
    }, 1000);
    return () => clearInterval(timer);
  }, [pageContent.bannerEndDate]);

  // Editor mode listener
  useEffect(() => {
    if (!isEditorMode) return;
    const handleMessage = (event) => {
      if (event.origin !== window.location.origin) return;
      if (event.data.type === 'EDITOR_UPDATE' || event.data.type === 'EDITOR_INIT') {
        if (event.data.payload?.data) {
          setPageContent(prev => ({ ...prev, ...event.data.payload.data }));
        }
      }
      if (event.data.type === 'SECTION_SELECTED') {
        setSelectedSection(event.data.sectionId);
      }
    };
    window.addEventListener('message', handleMessage);
    return () => window.removeEventListener('message', handleMessage);
  }, [isEditorMode]);

  const fetchPageContent = async () => {
    try {
      // Fetch landing page content
      const response = await pagesAPI.getOne('landing');
      if (response.data.data?.content) {
        setPageContent(prev => ({ ...prev, ...response.data.data.content }));
      }
    } catch (error) {
      console.log('Using default content for landing page');
    }
  };

  const [formSubmitting, setFormSubmitting] = useState(false);
  const [formSuccess, setFormSuccess] = useState(false);
  const [formError, setFormError] = useState('');

  const handleFormSubmit = async (e) => {
    e.preventDefault();
    setFormSubmitting(true);
    setFormError('');

    try {
      await contactsAPI.create({
        name: formData.fullName,
        email: formData.email,
        company: formData.companyName,
        phone: formData.phone,
        message: formData.message,
        subject: `New Inquiry from ${formData.fullName}`,
        source: 'Landing Page - Book A Call',
      });
      setFormSuccess(true);
      setFormData({ fullName: '', email: '', companyName: '', phone: '', message: '' });
    } catch (err) {
      setFormError(err.response?.data?.message || 'Something went wrong. Please try again.');
    } finally {
      setFormSubmitting(false);
    }
  };

  // Helper for image URLs
  const getImageUrl = (path) => {
    if (!path) return '';
    if (path.startsWith('http')) return path;
    const baseUrl = import.meta.env.VITE_API_URL || 'http://localhost:5000';
    return `${baseUrl}${path}`;
  };

  const formTags = pageContent.formTags?.split(',').map(t => t.trim()).filter(t => t) || [];

  return (
    <div style={{ minHeight: '100vh', backgroundColor: '#fff', fontFamily: "'Plus Jakarta Sans', 'Inter', sans-serif", overflowX: 'hidden', width: '100%', boxSizing: 'border-box' }}>
      {/* Top Banner */}
      {pageContent.showBanner === 'yes' && (
        <EditableSection sectionId="topBanner" label="Top Banner" isEditorMode={isEditorMode} isSelected={selectedSection === 'topBanner'}>
          <div style={{
            backgroundColor: '#2558BF',
            padding: isMobile ? '8px 20px' : '10px 24px',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            gap: isMobile ? '16px' : '20px',
          }}>
            <span style={{ color: '#fff', fontSize: isMobile ? '12px' : '14px', fontWeight: 400 }}>{pageContent.bannerText}</span>
            <div style={{ display: 'flex', gap: '6px' }}>
              {[
                { value: countdown.days, label: 'Days' },
                { value: countdown.hours, label: 'Hours' },
                { value: countdown.mins, label: 'Mins' },
                { value: countdown.secs, label: 'Sec' },
              ].map((item, i) => (
                <div key={i} style={{
                  backgroundColor: '#fff',
                  padding: '4px 6px',
                  borderRadius: '3px',
                  textAlign: 'center',
                  minWidth: '32px',
                }}>
                  <div style={{ fontSize: '13px', fontWeight: 700, color: '#000', lineHeight: 1.2 }}>{String(item.value).padStart(2, '0')}</div>
                  <div style={{ fontSize: '7px', color: '#333', textTransform: 'capitalize' }}>{item.label}</div>
                </div>
              ))}
            </div>
          </div>
        </EditableSection>
      )}

      {/* Hero + Trusted By Section - Combined background */}
      <div style={{
        backgroundColor: '#F9F3E6',
        minHeight: isMobile ? '1606px' : '1566px',
        width: '100%',
        boxSizing: 'border-box',
      }}>
        {/* Hero Section */}
        <EditableSection sectionId="hero" label="Hero Section" isEditorMode={isEditorMode} isSelected={selectedSection === 'hero'}>
          <section style={{
            padding: isMobile ? '24px 20px 32px' : '0',
            position: 'relative',
            width: '100%',
            boxSizing: 'border-box',
          }}>
            {/* Logo - Centered */}
            <div style={{
              textAlign: 'center',
              paddingTop: isMobile ? '24px' : '32px',
              marginBottom: isMobile ? '24px' : '70px',
            }}>
              <h1 style={{
                fontFamily: "'Archivo Black', sans-serif",
                fontSize: isMobile ? '34px' : '52.5px',
                fontStyle: 'normal',
                fontWeight: 400,
                letterSpacing: isMobile ? '-0.4813px' : '-0.6952px',
                lineHeight: isMobile ? '29.077px' : '42px',
                color: '#0A0A0A',
                margin: 0,
              }}>GOTI</h1>
            </div>

            {/* Content Container */}
            <div style={{
              display: 'flex',
              flexDirection: isMobile ? 'column' : 'row',
              alignItems: isMobile ? 'center' : 'flex-start',
              justifyContent: 'space-between',
              paddingLeft: isMobile ? '0' : '63px',
              paddingRight: isMobile ? '0' : '63px',
            }}>
              {/* Left Content */}
              <div style={{
                flex: '0 0 auto',
                width: isMobile ? '100%' : '586px',
                textAlign: isMobile ? 'center' : 'left',
              }}>
                {/* Title */}
                <h2 style={{
                  fontFamily: "'Inter', sans-serif",
                  fontSize: isMobile ? '28px' : '48px',
                  fontWeight: 600,
                  lineHeight: isMobile ? '34px' : 'normal',
                  color: '#1f1f1f',
                  letterSpacing: '0',
                  margin: 0,
                  marginBottom: isMobile ? '16px' : '28px',
                }}>
                  <span style={{ fontWeight: 600 }}>We Build Shopify</span><br />
                  <span style={{ fontWeight: 600 }}>Websites That Don't Just</span><br />
                  <span style={{ fontWeight: 600 }}>Look Good</span>
                  <span style={{ fontWeight: 500 }}> — </span>
                  <span style={{
                    fontWeight: 700,
                    position: 'relative',
                    display: 'inline',
                    backgroundImage: 'linear-gradient(transparent 70%, #E2775A 70%, #E2775A 92%, transparent 92%)',
                    backgroundRepeat: 'no-repeat',
                    backgroundSize: '100% 100%',
                  }}>
                    {pageContent.heroTitleHighlight}
                  </span>
                </h2>

                {/* Description */}
                <p style={{
                  fontFamily: "'Inter', sans-serif",
                  fontSize: isMobile ? '14px' : '20px',
                  fontWeight: 400,
                  color: '#606060',
                  lineHeight: isMobile ? '22px' : '28px',
                  width: isMobile ? '100%' : '562px',
                  margin: isMobile ? '0 auto' : 0,
                  marginBottom: isMobile ? '20px' : '24px',
                }}>
                  {pageContent.heroDescription}
                </p>

                {/* Button + Stats Container */}
                <div style={{
                  display: 'flex',
                  flexDirection: 'column',
                  alignItems: isMobile ? 'center' : 'flex-start',
                  gap: isMobile ? '24px' : '16px',
                  width: isMobile ? '100%' : '516px'
                }}>
                  {/* Button */}
                  <Link to={pageContent.heroButtonLink || '/contact'} style={{
                    display: 'inline-flex',
                    alignItems: 'center',
                    gap: '6px',
                    backgroundColor: isMobile ? '#E1FFA0' : '#2558BF',
                    border: '1px solid #000',
                    color: isMobile ? '#000' : '#fff',
                    padding: isMobile ? '10px 16px' : '12px 18px',
                    borderRadius: '300px',
                    textDecoration: 'none',
                    fontFamily: "'Inter', sans-serif",
                    fontSize: isMobile ? '18px' : '20px',
                    fontWeight: 700,
                    lineHeight: '22.734px',
                    width: 'fit-content',
                  }}>
                    {pageContent.heroButtonText}
                    <div style={{
                      width: isMobile ? '33.55px' : '42px',
                      height: isMobile ? '33.55px' : '42px',
                      display: 'flex',
                      alignItems: 'center',
                      justifyContent: 'center',
                    }}>
                      <svg width={isMobile ? '24' : '30'} height={isMobile ? '24' : '30'} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" style={{ transform: 'rotate(-45deg)' }}>
                        <path d="M5 12h14M12 5l7 7-7 7" />
                      </svg>
                    </div>
                  </Link>

                  {/* Hero Image - Mobile Only (between button and stats) */}
                  {isMobile && (
                    <div style={{
                      backgroundColor: '#C4C4C4',
                      borderRadius: '16px',
                      height: '200px',
                      width: '100%',
                      overflow: 'hidden',
                    }}>
                      {(() => {
                        const mediaUrl = pageContent.heroMedia || pageContent.heroImage;
                        if (!mediaUrl) return null;
                        const isVideo = ['.mp4', '.webm', '.ogg', '.mov'].some(ext =>
                          mediaUrl.toLowerCase().includes(ext)
                        );
                        if (isVideo) {
                          return (
                            <video src={getImageUrl(mediaUrl)} autoPlay muted loop playsInline style={{ width: '100%', height: '100%', objectFit: 'cover' }} />
                          );
                        }
                        return <img src={getImageUrl(mediaUrl)} alt="Hero" style={{ width: '100%', height: '100%', objectFit: 'cover' }} />;
                      })()}
                    </div>
                  )}

                  {/* Stats */}
                  <div style={{
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: isMobile ? 'center' : 'flex-start',
                    height: isMobile ? 'auto' : '76px',
                    width: '100%',
                  }}>
                    {[
                      { value: pageContent.stat1Value, label: pageContent.stat1Label },
                      { value: pageContent.stat2Value, label: pageContent.stat2Label },
                      { value: pageContent.stat3Value, label: pageContent.stat3Label },
                    ].map((stat, i) => (
                      <div key={i} style={{ display: 'flex', alignItems: 'center' }}>
                        {i > 0 && (
                          <div style={{
                            width: '1px',
                            height: isMobile ? '40px' : '48px',
                            backgroundColor: '#D1D5DC',
                            marginLeft: isMobile ? '12px' : '32px',
                            marginRight: isMobile ? '12px' : '32px',
                          }} />
                        )}
                        <div style={{
                          display: 'flex',
                          flexDirection: 'column',
                          alignItems: isMobile ? 'center' : 'flex-start',
                          gap: '2px',
                        }}>
                          <div style={{
                            fontFamily: "'Inter', sans-serif",
                            fontSize: isMobile ? '18px' : '27px',
                            fontWeight: 700,
                            color: '#1f1f1f',
                            lineHeight: isMobile ? '24px' : '36px',
                            whiteSpace: 'nowrap',
                          }}>{stat.value}</div>
                          <div style={{
                            fontFamily: "'Inter', sans-serif",
                            fontSize: isMobile ? '10px' : '14px',
                            fontWeight: 500,
                            color: '#6b6b6b',
                            lineHeight: isMobile ? '14px' : '20px',
                            whiteSpace: 'nowrap',
                          }}>{stat.label}</div>
                        </div>
                      </div>
                    ))}
                  </div>
                </div>
              </div>

              {/* Right Media Placeholder - Desktop Only */}
              {!isMobile && (
              <div style={{
                backgroundColor: '#C4C4C4',
                borderRadius: '24px',
                height: '450px',
                width: '665px',
                flexShrink: 0,
                overflow: 'hidden',
              }}>
                {(() => {
                  const mediaUrl = pageContent.heroMedia || pageContent.heroImage;
                  if (!mediaUrl) return null;

                  const isVideo = ['.mp4', '.webm', '.ogg', '.mov'].some(ext =>
                    mediaUrl.toLowerCase().includes(ext)
                  );

                  if (isVideo) {
                    return (
                      <video
                        src={getImageUrl(mediaUrl)}
                        autoPlay
                        muted
                        loop
                        playsInline
                        style={{
                          width: '100%',
                          height: '100%',
                          objectFit: 'cover',
                        }}
                      />
                    );
                  }

                  return (
                    <img
                      src={getImageUrl(mediaUrl)}
                      alt="Hero"
                      style={{
                        width: '100%',
                        height: '100%',
                        objectFit: 'cover',
                      }}
                    />
                  );
                })()}
              </div>
              )}
            </div>
          </section>
        </EditableSection>

        {/* Trusted By Section */}
        <EditableSection sectionId="trustedBy" label="Trusted By Section" isEditorMode={isEditorMode} isSelected={selectedSection === 'trustedBy'}>
          <section style={{ padding: isMobile ? '0 20px 48px' : '40px 63px 60px', marginTop: isMobile ? '0' : '50px', width: '100%', boxSizing: 'border-box' }}>
            <div style={{ maxWidth: isMobile ? '381px' : '1314px', margin: '0 auto', textAlign: 'center' }}>
              {/* Title - Figma: 18px, 700, #1F1F1F, line-height 26px, letter-spacing -0.15px, width 263px */}
              <p style={{
                fontFamily: "'Inter', sans-serif",
                fontSize: isMobile ? '18px' : '20px',
                fontWeight: 700,
                color: '#1F1F1F',
                letterSpacing: '-0.15px',
                lineHeight: isMobile ? '26px' : '28px',
                margin: '0 auto',
                marginBottom: isMobile ? '26px' : '12px',
                width: isMobile ? '263px' : 'auto',
                textAlign: 'center',
              }}>{pageContent.trustedByTitle}</p>

              {/* Arrow down indicator - Desktop only */}
              {!isMobile && (
                <div style={{
                  display: 'flex',
                  justifyContent: 'center',
                  marginBottom: '20px',
                }}>
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#1f1f1f" strokeWidth="2.5">
                    <path d="M6 9l6 6 6-6" />
                  </svg>
                </div>
              )}

              {/* Badges - Mobile: 3 rows layout */}
              {isMobile ? (
                <div style={{
                  display: 'flex',
                  flexDirection: 'column',
                  gap: '20px',
                  marginBottom: '26px',
                  fontFamily: "'Inter', sans-serif",
                  fontSize: '16px',
                  fontWeight: 600,
                  color: '#000000',
                  lineHeight: '24px',
                  letterSpacing: '-0.3125px',
                }}>
                  {/* Row 1: Shopify Plus Partner + Inc. 5000 */}
                  <div style={{
                    display: 'flex',
                    justifyContent: 'space-between',
                    alignItems: 'center',
                    width: '100%',
                  }}>
                    <span>{pageContent.badge1Text}</span>
                    <span style={{ fontStyle: 'italic' }}>{pageContent.badge3Text}</span>
                  </div>

                  {/* Row 2: Featured on Forbes - center */}
                  <div style={{
                    display: 'flex',
                    justifyContent: 'center',
                    width: '100%',
                  }}>
                    <span>{pageContent.badge2Text}</span>
                  </div>

                  {/* Row 3: E-commerce Expert + Best Design Award */}
                  <div style={{
                    display: 'flex',
                    justifyContent: 'space-between',
                    alignItems: 'center',
                    width: '100%',
                  }}>
                    <span>{pageContent.badge5Text}</span>
                    <span>{pageContent.badge4Text}</span>
                  </div>
                </div>
              ) : null}

              {/* Reviews Card - Mobile: white bg, border 1.5px, rounded 12px, px-24 py-18 */}
              <div style={{
                backgroundColor: '#FFFFFF',
                border: isMobile ? '1.5px solid #000000' : '2px solid #000000',
                borderRadius: isMobile ? '12px' : '24px',
                padding: isMobile ? '18px 24px' : '36px 56px',
                maxWidth: isMobile ? '100%' : '1046px',
                margin: '0 auto',
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                gap: isMobile ? '0' : '25px',
                position: 'relative',
                overflow: 'visible',
              }}>
                {/* Decorative Image - Top Right Corner Outside */}
                <img
                  src="/reviews-decor.png"
                  alt=""
                  style={{
                    position: 'absolute',
                    top: isMobile ? '-18px' : '-25px',
                    right: isMobile ? '-25px' : '-32px',
                    width: isMobile ? '33.084px' : '46px',
                    height: isMobile ? '26.867px' : '37px',
                    pointerEvents: 'none',
                  }}
                />
                {/* Badges Row - Desktop only */}
                {!isMobile && (
                  <div style={{
                    display: 'flex',
                    flexWrap: 'wrap',
                    justifyContent: 'center',
                    alignItems: 'center',
                    gap: '55px',
                  }}>
                    {[pageContent.badge1Text, pageContent.badge2Text, pageContent.badge3Text, pageContent.badge4Text, pageContent.badge5Text].filter(b => b).map((badge, i) => (
                      <span key={i} style={{
                        fontFamily: "'Inter', sans-serif",
                        fontSize: '16px',
                        fontWeight: 600,
                        color: '#000000',
                        lineHeight: '24px',
                        letterSpacing: '-0.31px',
                      }}>{badge}</span>
                    ))}
                  </div>
                )}

                {/* Reviews Row (Desktop) / Column (Mobile) */}
                <div style={{
                  display: 'flex',
                  flexDirection: isMobile ? 'column' : 'row',
                  justifyContent: 'center',
                  alignItems: 'center',
                  gap: isMobile ? '14px' : '40px',
                  width: isMobile ? '296px' : 'auto',
                }}>
                  {[
                    { rating: pageContent.review1Rating, platform: 'on Google', reviews: '(127 reviews)' },
                    { rating: pageContent.review2Rating, platform: 'on Trustpilot', reviews: '(127 reviews)' },
                    { rating: pageContent.review3Rating, platform: 'on Clutch', reviews: '(127 reviews)' },
                  ].filter(r => r.rating).map((review, i) => (
                    <div key={i} style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
                      {/* Stars - Figma: 16x16 each, 80px total width */}
                      <div style={{ display: 'flex', width: '80px', height: '16px' }}>
                        {[1,2,3,4,5].map(star => (
                          <svg key={star} width="16" height="16" viewBox="0 0 24 24" fill="#F5A623">
                            <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
                          </svg>
                        ))}
                      </div>
                      {/* Rating and source - Figma: 14px, letter-spacing -0.1504px */}
                      <div style={{ display: 'flex', alignItems: 'center', gap: '4px' }}>
                        <span style={{
                          fontFamily: "'Inter', sans-serif",
                          fontSize: '14px',
                          fontWeight: 700,
                          color: '#0a0a0a',
                          lineHeight: '20px',
                          letterSpacing: '-0.1504px',
                        }}>{review.rating}</span>
                        <span style={{
                          fontFamily: "'Inter', sans-serif",
                          fontSize: '14px',
                          fontWeight: 400,
                          color: '#0a0a0a',
                          lineHeight: '20px',
                          letterSpacing: '-0.1504px',
                        }}>{review.platform}</span>
                        <span style={{
                          fontFamily: "'Inter', sans-serif",
                          fontSize: '12px',
                          fontWeight: 400,
                          color: '#000000',
                          lineHeight: '16px',
                        }}>{review.reviews}</span>
                      </div>
                    </div>
                  ))}
                </div>
              </div>
            </div>
          </section>
        </EditableSection>
      </div>

      {/* Portfolio Section */}
      <EditableSection sectionId="portfolio" label="Portfolio Section" isEditorMode={isEditorMode} isSelected={selectedSection === 'portfolio'}>
        <section style={{
          padding: isMobile ? '32px 20px' : '60px 63px',
          backgroundColor: '#FFFDF9',
          width: '100%',
          boxSizing: 'border-box',
        }}>
          <div style={{ maxWidth: '1314px', margin: '0 auto' }}>
            <div style={{
              display: 'grid',
              gridTemplateColumns: isMobile ? 'repeat(2, 1fr)' : 'repeat(4, 1fr)',
              gap: isMobile ? '12px' : '24px',
            }}>
              {(pageContent.portfolioImages?.length > 0 ? pageContent.portfolioImages : [1,2,3,4]).slice(0, isMobile ? 2 : 4).map((img, i) => (
                <div key={i} style={{ textAlign: 'center' }}>
                  <div style={{
                    backgroundColor: '#fff',
                    borderRadius: isMobile ? '12px' : '16px',
                    height: isMobile ? '200px' : '450px',
                    marginBottom: isMobile ? '8px' : '12px',
                    overflow: 'hidden',
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                  }}>
                    {typeof img === 'string' && img ? (
                      <img src={getImageUrl(img)} alt={`Portfolio ${i+1}`} style={{ width: '100%', height: '100%', objectFit: 'cover' }} />
                    ) : (
                      <span style={{ color: '#ccc', fontSize: isMobile ? '12px' : '14px' }}>Portfolio {i+1}</span>
                    )}
                  </div>
                  <p style={{
                    fontFamily: "'Plus Jakarta Sans', 'Inter', sans-serif",
                    fontSize: isMobile ? '12px' : '16px',
                    fontWeight: 700,
                    fontStyle: 'normal',
                    color: '#000',
                    textAlign: 'center',
                    lineHeight: isMobile ? '14px' : '17.614px',
                    letterSpacing: '-0.229px',
                    margin: 0,
                  }}>{pageContent.portfolioCategory}</p>
                </div>
              ))}
            </div>
          </div>
        </section>
      </EditableSection>

      {/* Real Numbers Section */}
      <EditableSection sectionId="stats" label="Real Numbers Section" isEditorMode={isEditorMode} isSelected={selectedSection === 'stats'}>
        <section style={{
          padding: isMobile ? '32px 20px' : '60px 63px',
          backgroundColor: '#FFFDF9',
          width: '100%',
          boxSizing: 'border-box',
        }}>
          <div style={{ maxWidth: '1314px', margin: '0 auto', textAlign: 'center' }}>
            {/* Live Stats Badge */}
            <span style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: isMobile ? '6px' : '8px',
              backgroundColor: '#2558BF',
              color: '#fff',
              padding: isMobile ? '5px 12px' : '6px 14px',
              borderRadius: '100px',
              fontSize: isMobile ? '14px' : '16px',
              fontWeight: 500,
              marginBottom: isMobile ? '12px' : '14px',
            }}>
              <span style={{
                width: '8px',
                height: '8px',
                backgroundColor: '#fff',
                borderRadius: '50%',
              }} />
              Live Stats
            </span>

            {/* Title */}
            <h2 style={{
              fontFamily: "'Inter', sans-serif",
              fontSize: isMobile ? '28px' : '48px',
              fontWeight: 700,
              color: '#000',
              lineHeight: isMobile ? '34px' : '48px',
              margin: 0,
              marginBottom: isMobile ? '24px' : '40px',
            }}>{pageContent.statsTitle}</h2>

            {/* Stats Grid */}
            <div style={{
              display: 'grid',
              gridTemplateColumns: isMobile ? 'repeat(2, 1fr)' : 'repeat(4, 1fr)',
              gap: isMobile ? '12px' : '24px',
            }}>
              {[
                { value: pageContent.statLive1Value, label: pageContent.statLive1Label, icon: pageContent.statLive1Icon, live: true },
                { value: pageContent.statLive2Value, label: pageContent.statLive2Label, icon: pageContent.statLive2Icon, live: true },
                { value: pageContent.statLive3Value, label: pageContent.statLive3Label, icon: pageContent.statLive3Icon },
                { value: pageContent.statLive4Value, label: pageContent.statLive4Label, icon: pageContent.statLive4Icon },
              ].map((stat, i) => (
                <div key={i} style={{
                  border: '1.5px solid #000',
                  borderRadius: isMobile ? '12px' : '16px',
                  padding: isMobile ? '16px' : '24px',
                  position: 'relative',
                  backgroundColor: '#fff',
                  height: isMobile ? 'auto' : '198px',
                  textAlign: 'left',
                }}>
                  {/* Icon Container */}
                  <div style={{
                    width: isMobile ? '36px' : '48px',
                    height: isMobile ? '36px' : '48px',
                    backgroundColor: '#E1FFA0',
                    borderRadius: isMobile ? '10px' : '14px',
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    marginBottom: isMobile ? '12px' : '16px',
                  }}>
                    {stat.icon ? (
                      <img src={getImageUrl(stat.icon)} alt="" style={{ width: isMobile ? '18px' : '24px', height: isMobile ? '18px' : '24px' }} />
                    ) : (
                      <span style={{ fontSize: isMobile ? '16px' : '20px' }}>
                        {i === 0 ? '🛒' : i === 1 ? '👥' : i === 2 ? '📈' : '⭐'}
                      </span>
                    )}
                  </div>

                  {/* Live Badge */}
                  {stat.live && (
                    <span style={{
                      position: 'absolute',
                      top: isMobile ? '10px' : '12px',
                      right: isMobile ? '10px' : '12px',
                      display: 'flex',
                      alignItems: 'center',
                      gap: '4px',
                    }}>
                      <span style={{
                        width: isMobile ? '5px' : '6px',
                        height: isMobile ? '5px' : '6px',
                        backgroundColor: '#DF0505',
                        borderRadius: '50%',
                        opacity: 0.79,
                      }} />
                      <span style={{
                        fontFamily: "'Inter', sans-serif",
                        fontSize: isMobile ? '12px' : '14px',
                        fontWeight: 400,
                        color: '#DF0505',
                      }}>Live</span>
                    </span>
                  )}

                  {/* Value */}
                  <div style={{
                    fontFamily: "'Inter', sans-serif",
                    fontSize: isMobile ? '22px' : '36px',
                    fontWeight: 700,
                    color: '#000',
                    lineHeight: isMobile ? '28px' : '40px',
                    letterSpacing: '0.37px',
                    marginBottom: '4px',
                  }}>{stat.value}</div>

                  {/* Label */}
                  <div style={{
                    fontFamily: "'Inter', sans-serif",
                    fontSize: isMobile ? '12px' : '14px',
                    fontWeight: 500,
                    color: '#1f1f1f',
                    lineHeight: isMobile ? '14px' : '16px',
                    letterSpacing: '-0.15px',
                  }}>{stat.label}</div>
                </div>
              ))}
            </div>
          </div>
        </section>
      </EditableSection>

      {/* Hidden Cost Section */}
      <EditableSection sectionId="hiddenCost" label="Hidden Cost Section" isEditorMode={isEditorMode} isSelected={selectedSection === 'hiddenCost'}>
        <section style={{
          padding: isMobile ? '32px 20px' : '80px 63px',
          backgroundColor: '#FFFDF9',
          width: '100%',
          boxSizing: 'border-box',
        }}>
          <div style={{ maxWidth: '1314px', margin: '0 auto' }}>
            {/* Title - 620px fixed width, centered */}
            <h2 style={{
              fontFamily: "'Inter', sans-serif",
              fontSize: isMobile ? '24px' : '48px',
              fontWeight: 700,
              fontStyle: 'normal',
              textAlign: 'center',
              color: '#000',
              lineHeight: isMobile ? '32px' : 'normal',
              width: isMobile ? '100%' : '620px',
              margin: isMobile ? '0 auto 32px' : '0 auto 48px',
            }}>
              {pageContent.hiddenCostTitle}{' '}
              <span style={{
                backgroundImage: 'linear-gradient(transparent 70%, #E2775A 70%, #E2775A 92%, transparent 92%)',
                backgroundRepeat: 'no-repeat',
                backgroundSize: '100% 100%',
              }}>{pageContent.hiddenCostTitleHighlight}</span>
            </h2>

            {/* Cards Grid - 2x2 */}
            <div style={{
              display: 'grid',
              gridTemplateColumns: isMobile ? '1fr' : 'repeat(2, 620px)',
              gap: isMobile ? '16px' : '24px',
              justifyContent: 'center',
            }}>
              {[
                { title: pageContent.painPoint1Title, desc: pageContent.painPoint1Desc, stat: pageContent.painPoint1Stat },
                { title: pageContent.painPoint2Title, desc: pageContent.painPoint2Desc, stat: pageContent.painPoint2Stat },
                { title: pageContent.painPoint3Title, desc: pageContent.painPoint3Desc, stat: pageContent.painPoint3Stat },
                { title: pageContent.painPoint4Title, desc: pageContent.painPoint4Desc, stat: pageContent.painPoint4Stat },
              ].filter(p => p.title).map((point, i) => (
                <div key={i} style={{
                  backgroundColor: '#fff',
                  borderRadius: isMobile ? '12px' : '16px',
                  borderLeft: isMobile ? '3px solid #E2775A' : '4px solid #E2775A',
                  padding: isMobile ? '20px 16px' : '30px 32px',
                  boxShadow: '0px 1px 3px rgba(0,0,0,0.1), 0px 1px 2px rgba(0,0,0,0.1)',
                }}>
                  <div style={{ display: 'flex', gap: isMobile ? '8px' : '10px', alignItems: 'flex-start' }}>
                    {/* Icon Circle */}
                    <div style={{
                      backgroundColor: '#E2775A',
                      width: isMobile ? '32px' : '40px',
                      height: isMobile ? '32px' : '40px',
                      borderRadius: '50%',
                      display: 'flex',
                      alignItems: 'center',
                      justifyContent: 'center',
                      flexShrink: 0,
                    }}>
                      <svg width={isMobile ? '18' : '24'} height={isMobile ? '18' : '24'} viewBox="0 0 24 24" fill="none" stroke="#fff" strokeWidth="2.5">
                        <path d="M18 6L6 18M6 6l12 12" />
                      </svg>
                    </div>

                    {/* Content */}
                    <div style={{ flex: 1, display: 'flex', flexDirection: 'column', gap: isMobile ? '10px' : '14px' }}>
                      {/* Title & Description */}
                      <div style={{ display: 'flex', flexDirection: 'column', gap: isMobile ? '4px' : '6px' }}>
                        <h4 style={{
                          fontFamily: "'Inter', sans-serif",
                          fontSize: isMobile ? '16px' : '20px',
                          fontWeight: 600,
                          lineHeight: isMobile ? '22px' : '28px',
                          letterSpacing: '-0.4492px',
                          color: '#0A0A0A',
                          margin: 0,
                        }}>{point.title}</h4>
                        <p style={{
                          fontFamily: "'Inter', sans-serif",
                          fontSize: isMobile ? '14px' : '16px',
                          fontWeight: 500,
                          lineHeight: isMobile ? '20px' : '24px',
                          color: '#4A5565',
                          margin: 0,
                        }}>{point.desc}</p>
                      </div>

                      {/* Stat Badge */}
                      <div style={{
                        display: 'inline-flex',
                        alignItems: 'center',
                        justifyContent: 'center',
                        backgroundColor: '#FEF2F2',
                        padding: '6px 12px',
                        borderRadius: '100px',
                        width: 'fit-content',
                      }}>
                        <span style={{
                          fontFamily: "'Inter', sans-serif",
                          fontSize: '14px',
                          fontWeight: 500,
                          lineHeight: '20px',
                          letterSpacing: '-0.1504px',
                          color: '#E2775A',
                        }}>
                          📊 {point.stat}
                        </span>
                      </div>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </section>
      </EditableSection>

      {/* Sounds Familiar Section */}
      <EditableSection sectionId="familiar" label="Sounds Familiar Section" isEditorMode={isEditorMode} isSelected={selectedSection === 'familiar'}>
        <section style={{
          padding: isMobile ? '32px 20px' : '80px 63px',
          backgroundColor: '#FFFDF9',
          width: '100%',
          boxSizing: 'border-box',
        }}>
          <div style={{ maxWidth: '1100px', margin: '0 auto', textAlign: 'center', position: 'relative' }}>
            {/* "SOUNDS FAMILIAR?" badge with arrow - Mobile version - Above title, right aligned */}
            {isMobile && (
              <div style={{
                display: 'flex',
                flexDirection: 'row',
                alignItems: 'center',
                justifyContent: 'flex-end',
                gap: '6px',
                marginBottom: '12px',
                paddingRight: '10px',
              }}>
                <img
                  src={arrow12Image}
                  alt=""
                  style={{
                    width: '70.94px',
                    height: '29.437px',
                  }}
                />
                <span style={{
                  fontFamily: "'Caveat', 'Caveat', cursive",
                  fontSize: '15.544px',
                  fontWeight: 400,
                  fontStyle: 'normal',
                  color: '#000',
                  textAlign: 'center',
                  lineHeight: 'normal',
                  transform: 'rotate(-6.171deg)',
                  whiteSpace: 'nowrap',
                  marginLeft: '-35px',
                }}>SOUNDS FAMILIAR?</span>
              </div>
            )}

            {/* Title with highlight */}
            <h2 style={{
              fontFamily: "'Inter', sans-serif",
              fontSize: isMobile ? '22px' : '36px',
              fontWeight: 600,
              color: '#000',
              margin: isMobile ? '0 0 16px 0' : '0 0 24px 0',
              lineHeight: isMobile ? 1.3 : 1.4,
              position: 'relative',
              display: 'inline-block',
            }}>
              {pageContent.familiarTitle}
              <br />
              <span style={{
                backgroundColor: '#D5FF79',
                padding: '2px 8px',
                borderRadius: '4px',
                display: 'inline',
              }}>{pageContent.familiarTitleHighlight}</span>
            </h2>

            {/* "SOUNDS FAMILIAR?" badge with arrow - Desktop version */}
            {!isMobile && (
              <div style={{
                position: 'absolute',
                top: '40px',
                right: '-180px',
                display: 'flex',
                flexDirection: 'row',
                alignItems: 'flex-start',
                gap: '10px',
              }}>
                <img
                  src={arrowImage}
                  alt=""
                  style={{
                    width: '85px',
                    height: 'auto',
                    marginTop: '20px',
                  }}
                />
                <span style={{
                  fontFamily: "'Caveat', 'Caveat', cursive",
                  fontSize: '24px',
                  fontWeight: 400,
                  fontStyle: 'normal',
                  color: '#000',
                  textAlign: 'left',
                  lineHeight: '1.1',
                  letterSpacing: '-0.5px',
                  whiteSpace: 'nowrap',
                }}>SOUNDS<br />FAMILIAR?</span>
              </div>
            )}

            {/* Description */}
            <p style={{
              fontFamily: "'Inter', sans-serif",
              fontSize: isMobile ? '14px' : '18px',
              fontWeight: 400,
              color: '#4A5565',
              lineHeight: isMobile ? '22px' : '28px',
              margin: 0,
              maxWidth: '780px',
              marginLeft: 'auto',
              marginRight: 'auto',
            }}>{pageContent.familiarDescription}</p>
          </div>
        </section>
      </EditableSection>

      {/* Comparison Section */}
      <EditableSection sectionId="comparison" label="Comparison Section" isEditorMode={isEditorMode} isSelected={selectedSection === 'comparison'}>
        <section style={{
          padding: isMobile ? '32px 20px' : '60px 63px',
          backgroundColor: '#FFFDF9',
          width: '100%',
          boxSizing: 'border-box',
        }}>
          <div style={{ maxWidth: '1100px', margin: '0 auto' }}>
            {/* Move all containers 60px upward */}
            <div style={{
              display: 'flex',
              flexDirection: isMobile ? 'column' : 'row',
              alignItems: 'center',
              justifyContent: 'center',
              position: 'relative',
              marginTop: isMobile ? '0' : '-60px',
            }}>
              {/* Column 1 - DIY with Templates */}
              <div style={{
                borderRadius: isMobile ? '12px 12px 0 0' : '16px',
                padding: isMobile ? '55px 20px' : '32px',
                backgroundColor: '#fff',
                border: '1.5px solid #000',
                borderBottom: isMobile ? 'none' : '1.5px solid #000',
                width: isMobile ? 'calc(100% - 30px)' : '340px',
                flexShrink: 0,
                position: 'relative',
                zIndex: 1,
                marginRight: isMobile ? '0' : '-20px',
                marginBottom: isMobile ? '-20px' : '0',
              }}>
                <h3 style={{
                  fontFamily: "'Inter', sans-serif",
                  fontSize: isMobile ? '18px' : '24px',
                  fontWeight: 700,
                  color: '#0a0a0a',
                  margin: 0,
                  marginBottom: isMobile ? '6px' : '8px',
                  textAlign: 'center',
                  lineHeight: isMobile ? '24px' : '32px',
                  letterSpacing: '0.07px',
                }}>{pageContent.col1Title}</h3>
                <p style={{
                  fontFamily: "'Inter', sans-serif",
                  fontSize: isMobile ? '12px' : '14px',
                  fontWeight: 400,
                  color: '#4a5565',
                  margin: 0,
                  marginBottom: isMobile ? '16px' : '24px',
                  textAlign: 'center',
                  letterSpacing: '-0.15px',
                }}>{pageContent.col1Subtitle}</p>
                <div style={{ display: 'flex', flexDirection: 'column', gap: isMobile ? '10px' : '12px' }}>
                  {pageContent.col1Items?.split('\n').filter(i => i).map((item, i) => (
                    <div key={i} style={{
                      display: 'flex',
                      alignItems: 'center',
                      gap: isMobile ? '10px' : '12px',
                    }}>
                      <svg width={isMobile ? '16' : '20'} height={isMobile ? '16' : '20'} viewBox="0 0 20 20" fill="none" style={{ flexShrink: 0 }}>
                        <path d="M15 5L5 15M5 5L15 15" stroke="#EF4444" strokeWidth="2" strokeLinecap="round"/>
                      </svg>
                      <span style={{
                        fontFamily: "'Inter', sans-serif",
                        fontSize: isMobile ? '14px' : '16px',
                        fontWeight: 500,
                        color: '#6a7282',
                        lineHeight: isMobile ? '18px' : '20px',
                        letterSpacing: '-0.15px',
                      }}>{item}</span>
                    </div>
                  ))}
                </div>
              </div>

              {/* Column 2 - GOTI Premium (Featured - overlaps both sides) */}
              <div style={{
                borderRadius: '16px',
                padding: isMobile ? '34px' : '32px 40px',
                backgroundColor: '#2558BF',
                color: '#fff',
                position: 'relative',
                zIndex: 10,
                boxShadow: '0 25px 50px -12px rgba(0, 0, 0, 0.25)',
                width: isMobile ? '100%' : '445px',
                minHeight: isMobile ? '427px' : 'auto',
                flexShrink: 0,
                marginTop: isMobile ? '0' : '20px',
                marginBottom: isMobile ? '0' : '0',
                display: 'flex',
                flexDirection: 'column',
              }}>
                {/* Best Value Badge */}
                <div style={{
                  position: 'absolute',
                  top: '-12px',
                  left: '50%',
                  transform: 'translateX(-50%)',
                  backgroundColor: '#D5FF79',
                  color: '#000',
                  padding: isMobile ? '4px 17px' : '6px 18px',
                  borderRadius: '32px',
                  fontFamily: "'Inter', sans-serif",
                  fontSize: '14px',
                  fontWeight: 700,
                  whiteSpace: 'nowrap',
                  letterSpacing: '-0.15px',
                }}>Best Value</div>
                <h3 style={{
                  fontFamily: "'Inter', sans-serif",
                  fontSize: '24px',
                  fontWeight: 700,
                  color: '#fff',
                  margin: 0,
                  marginTop: isMobile ? '16px' : '12px',
                  marginBottom: '8px',
                  textAlign: 'center',
                  lineHeight: '32px',
                  letterSpacing: '0.07px',
                }}>{pageContent.col2Title}</h3>
                <p style={{
                  fontFamily: "'Inter', sans-serif",
                  fontSize: '16px',
                  fontWeight: 500,
                  color: '#f6f6f6',
                  margin: 0,
                  marginBottom: isMobile ? '24px' : '28px',
                  textAlign: 'center',
                  lineHeight: '20px',
                  letterSpacing: '-0.15px',
                }}>{pageContent.col2Subtitle}</p>
                <div style={{ display: 'flex', flexDirection: 'column', gap: '13px' }}>
                  {pageContent.col2Items?.split('\n').filter(i => i).map((item, i) => (
                    <div key={i} style={{
                      display: 'flex',
                      alignItems: 'center',
                      gap: '12px',
                    }}>
                      <svg width="21" height="21" viewBox="0 0 24 24" fill="none" stroke="#fff" strokeWidth="2.5" style={{ flexShrink: 0 }}>
                        <path d="M20 6L9 17l-5-5" />
                      </svg>
                      <span style={{
                        fontFamily: "'Inter', sans-serif",
                        fontSize: '18px',
                        fontWeight: 600,
                        color: '#fff',
                        lineHeight: '20px',
                        letterSpacing: '-0.15px',
                      }}>{item}</span>
                    </div>
                  ))}
                </div>
              </div>

              {/* Column 3 - Cheap Freelancers */}
              <div style={{
                borderRadius: isMobile ? '0 0 12px 12px' : '16px',
                padding: isMobile ? '55px 20px' : '32px',
                backgroundColor: '#fff',
                border: '1.5px solid #000',
                borderTop: isMobile ? 'none' : '1.5px solid #000',
                width: isMobile ? 'calc(100% - 30px)' : '340px',
                flexShrink: 0,
                position: 'relative',
                zIndex: 1,
                marginLeft: isMobile ? '0' : '-20px',
                marginTop: isMobile ? '-20px' : '0',
              }}>
                <h3 style={{
                  fontFamily: "'Inter', sans-serif",
                  fontSize: isMobile ? '18px' : '24px',
                  fontWeight: 700,
                  color: '#0a0a0a',
                  margin: 0,
                  marginBottom: isMobile ? '6px' : '8px',
                  textAlign: 'center',
                  lineHeight: isMobile ? '24px' : '32px',
                  letterSpacing: '0.07px',
                }}>{pageContent.col3Title}</h3>
                <p style={{
                  fontFamily: "'Inter', sans-serif",
                  fontSize: isMobile ? '12px' : '14px',
                  fontWeight: 400,
                  color: '#4a5565',
                  margin: 0,
                  marginBottom: isMobile ? '16px' : '24px',
                  textAlign: 'center',
                  letterSpacing: '-0.15px',
                }}>{pageContent.col3Subtitle}</p>
                <div style={{ display: 'flex', flexDirection: 'column', gap: isMobile ? '10px' : '12px' }}>
                  {pageContent.col3Items?.split('\n').filter(i => i).map((item, i) => (
                    <div key={i} style={{
                      display: 'flex',
                      alignItems: 'center',
                      gap: isMobile ? '10px' : '12px',
                    }}>
                      <svg width={isMobile ? '16' : '20'} height={isMobile ? '16' : '20'} viewBox="0 0 20 20" fill="none" style={{ flexShrink: 0 }}>
                        <path d="M15 5L5 15M5 5L15 15" stroke="#EF4444" strokeWidth="2" strokeLinecap="round"/>
                      </svg>
                      <span style={{
                        fontFamily: "'Inter', sans-serif",
                        fontSize: isMobile ? '14px' : '16px',
                        fontWeight: 500,
                        color: '#6a7282',
                        lineHeight: isMobile ? '18px' : '20px',
                        letterSpacing: '-0.15px',
                      }}>{item}</span>
                    </div>
                  ))}
                </div>
              </div>
            </div>
          </div>
        </section>
      </EditableSection>

      {/* Process Section */}
      <EditableSection sectionId="process" label="Our Process Section" isEditorMode={isEditorMode} isSelected={selectedSection === 'process'}>
        <section style={{
          padding: isMobile ? '32px 20px' : '70px 63px 80px',
          backgroundColor: '#F9F3E6',
          width: '100%',
          boxSizing: 'border-box',
        }}>
          <div style={{ maxWidth: '1314px', margin: '0 auto' }}>
            {/* Title */}
            <h2 style={{
              fontFamily: "'Inter', sans-serif",
              fontSize: isMobile ? '28px' : '48px',
              fontWeight: 700,
              color: '#000',
              textAlign: 'center',
              margin: 0,
              marginBottom: isMobile ? '12px' : '16px',
            }}>{pageContent.processTitle}</h2>
            {/* Subtitle */}
            <p style={{
              fontFamily: "'Inter', sans-serif",
              fontSize: isMobile ? '14px' : '24px',
              fontWeight: 500,
              color: '#1f1f1f',
              textAlign: 'center',
              margin: 0,
              marginBottom: isMobile ? '32px' : '48px',
              letterSpacing: '0.07px',
              lineHeight: isMobile ? '22px' : '36px',
            }}>{pageContent.processSubtitle}</p>

            {/* Mobile Image placeholder - shown before steps */}
            {isMobile && (
              <div style={{
                width: '100%',
                height: '300px',
                backgroundColor: '#bababa',
                borderRadius: '16px',
                marginBottom: '32px',
                overflow: 'hidden',
              }}>
                {pageContent.processImage && (
                  <img src={getImageUrl(pageContent.processImage)} alt="Process" style={{ width: '100%', height: '100%', objectFit: 'cover' }} />
                )}
              </div>
            )}

            <div style={{
              display: 'flex',
              flexDirection: isMobile ? 'column' : 'row',
              gap: isMobile ? '24px' : '42px',
              alignItems: 'flex-end',
            }}>
              {/* Steps */}
              <div style={{ width: isMobile ? '100%' : '592px', flexShrink: 0 }}>
                {[
                  { num: '01', title: pageContent.step1Title, desc: pageContent.step1Desc, icon: pageContent.step1Icon },
                  { num: '02', title: pageContent.step2Title, desc: pageContent.step2Desc, icon: pageContent.step2Icon },
                  { num: '03', title: pageContent.step3Title, desc: pageContent.step3Desc, icon: pageContent.step3Icon },
                  { num: '04', title: pageContent.step4Title, desc: pageContent.step4Desc, icon: pageContent.step4Icon },
                ].filter(s => s.title).map((step, i, arr) => (
                  <div key={i} style={{
                    padding: isMobile ? '12px 0' : '15px',
                    borderBottom: i < arr.length - 1 ? '1px solid #000' : 'none',
                  }}>
                    {/* Icon and Number Row */}
                    <div style={{
                      display: 'flex',
                      justifyContent: 'space-between',
                      alignItems: 'center',
                      marginBottom: isMobile ? '10px' : '15px',
                    }}>
                      {/* Icon Circle */}
                      <div style={{
                        width: isMobile ? '36px' : '42px',
                        height: isMobile ? '36px' : '42px',
                        borderRadius: isMobile ? '9px' : '10.5px',
                        backgroundColor: '#E2775A',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center',
                        flexShrink: 0,
                      }}>
                        {step.icon ? (
                          <img src={getImageUrl(step.icon)} alt="" style={{ width: isMobile ? '18px' : '21px', height: isMobile ? '18px' : '21px' }} />
                        ) : (
                          <svg width={isMobile ? '18' : '21'} height={isMobile ? '18' : '21'} viewBox="0 0 24 24" fill="none" stroke="#fff" strokeWidth="2">
                            {i === 0 && <path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72 12.84 12.84 0 00.7 2.81 2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45 12.84 12.84 0 002.81.7A2 2 0 0122 16.92z" />}
                            {i === 1 && <><path d="M12 19l7-7 3 3-7 7-3-3z" /><path d="M18 13l-1.5-7.5L2 2l3.5 14.5L13 18l5-5z" /><path d="M2 2l7.586 7.586" /><circle cx="11" cy="11" r="2" /></>}
                            {i === 2 && <><polyline points="16 18 22 12 16 6" /><polyline points="8 6 2 12 8 18" /></>}
                            {i === 3 && <path d="M22 2L11 13M22 2l-7 20-4-9-9-4 20-7z" />}
                          </svg>
                        )}
                      </div>
                      {/* Step Number */}
                      <span style={{
                        fontFamily: "'Montserrat', sans-serif",
                        fontSize: isMobile ? '28px' : '40px',
                        fontWeight: 600,
                        color: 'rgba(0, 0, 0, 0.25)',
                        lineHeight: 1,
                      }}>{step.num}</span>
                    </div>
                    {/* Title */}
                    <h4 style={{
                      fontFamily: "'Inter', sans-serif",
                      fontSize: isMobile ? '16px' : '20px',
                      fontWeight: 600,
                      color: '#0a0a0a',
                      margin: 0,
                      marginBottom: isMobile ? '5px' : '7px',
                      lineHeight: isMobile ? '22px' : '28px',
                    }}>{step.title}</h4>
                    {/* Description */}
                    <p style={{
                      fontFamily: "'Inter', sans-serif",
                      fontSize: isMobile ? '14px' : '16px',
                      fontWeight: 500,
                      color: i === 0 ? '#1f1f1f' : '#4a5565',
                      lineHeight: isMobile ? '20px' : '24px',
                      letterSpacing: '-0.19px',
                      margin: 0,
                    }}>{step.desc}</p>
                  </div>
                ))}
              </div>

              {/* Image placeholder */}
              {!isMobile && (
                <div style={{
                  width: '666px',
                  height: '696px',
                  backgroundColor: '#bababa',
                  borderRadius: '16px',
                  flexShrink: 0,
                  overflow: 'hidden',
                }}>
                  {pageContent.processImage && (
                    <img src={getImageUrl(pageContent.processImage)} alt="Process" style={{ width: '100%', height: '100%', objectFit: 'cover' }} />
                  )}
                </div>
              )}
            </div>

            {/* CTA Button */}
            <div style={{ textAlign: 'center', marginTop: isMobile ? '32px' : '50px' }}>
              <Link to="/contact" style={{
                display: 'inline-flex',
                alignItems: 'center',
                gap: '6px',
                backgroundColor: '#E1FFA0',
                color: '#000',
                padding: isMobile ? '10px 14px' : '12px 18px',
                borderRadius: '300px',
                border: '1px solid #000',
                textDecoration: 'none',
                fontFamily: "'Inter', sans-serif",
                fontSize: isMobile ? '16px' : '22px',
                fontWeight: 700,
                lineHeight: '22.734px',
              }}>
                {pageContent.processCTAText}
                <div style={{
                  width: isMobile ? '32px' : '42px',
                  height: isMobile ? '32px' : '42px',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                }}>
                  <svg width={isMobile ? '22' : '30'} height={isMobile ? '22' : '30'} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" style={{ transform: 'rotate(-45deg)' }}>
                    <path d="M5 12h14M12 5l7 7-7 7" />
                  </svg>
                </div>
              </Link>
            </div>
          </div>
        </section>
      </EditableSection>

      {/* Value Section */}
      <EditableSection sectionId="value" label="Value Proposition Section" isEditorMode={isEditorMode} isSelected={selectedSection === 'value'}>
        <section style={{
          padding: isMobile ? '32px 20px 42px' : '80px 63px',
          backgroundColor: '#F9F3E6',
          width: '100%',
          boxSizing: 'border-box',
        }}>
          <div style={{ maxWidth: '1314px', margin: '0 auto' }}>
            {/* Title with underline on highlight */}
            <div style={{ textAlign: 'center', marginBottom: isMobile ? '40px' : '70px', position: 'relative' }}>
              <h2 style={{
                fontFamily: "'Inter', sans-serif",
                fontSize: isMobile ? '24px' : '48px',
                fontWeight: 700,
                color: '#000',
                margin: 0,
                display: 'inline',
              }}>
                {pageContent.valueTitle}{' '}
                <span style={{ position: 'relative', display: 'inline-block' }}>
                  {pageContent.valueTitleHighlight}
                  <span style={{
                    position: 'absolute',
                    bottom: '2px',
                    left: 0,
                    width: '100%',
                    height: '11px',
                    backgroundColor: '#E2775A',
                    zIndex: -1,
                  }} />
                </span>
              </h2>
            </div>

            <div style={{
              display: 'flex',
              flexDirection: isMobile ? 'column' : 'row',
              gap: isMobile ? '24px' : '37px',
              alignItems: 'center',
              justifyContent: 'center',
            }}>
              {/* Pricing Card */}
              <div style={{
                width: isMobile ? '100vw' : '601px',
                marginLeft: isMobile ? '-20px' : '0',
                marginRight: isMobile ? '-20px' : '0',
                height: isMobile ? 'auto' : '647px',
                backgroundColor: '#FFFDF9',
                borderRadius: isMobile ? '0' : '15px',
                boxShadow: isMobile ? 'none' : '0px 27px 54px -13px rgba(0, 0, 0, 0.25)',
                padding: isMobile ? '20px 24px 28px' : '15px 43px 36px',
                position: 'relative',
                flexShrink: 0,
                order: isMobile ? 2 : 1,
              }}>
                {/* Top Icon */}
                <div style={{ textAlign: 'center', marginBottom: isMobile ? '16px' : '24px' }}>
                  <div style={{
                    width: isMobile ? '48px' : '60px',
                    height: isMobile ? '48px' : '60px',
                    backgroundColor: '#000',
                    borderRadius: '50%',
                    display: 'inline-flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                  }}>
                    <svg width={isMobile ? '26' : '34'} height={isMobile ? '26' : '34'} viewBox="0 0 24 24" fill="none" stroke="#fff" strokeWidth="2">
                      <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2" />
                    </svg>
                  </div>
                </div>

                {/* Worth Text */}
                <p style={{
                  fontFamily: "'Inter', sans-serif",
                  fontSize: isMobile ? '20px' : '26px',
                  fontWeight: 600,
                  color: '#000',
                  textAlign: 'center',
                  margin: 0,
                  marginBottom: isMobile ? '6px' : '8px',
                  letterSpacing: '0.08px',
                  lineHeight: isMobile ? '28px' : '34px',
                }}>{pageContent.worthText}</p>

                {/* Subtext */}
                <p style={{
                  fontFamily: "'Inter', sans-serif",
                  fontSize: isMobile ? '14px' : '17px',
                  fontWeight: 500,
                  color: '#4a5565',
                  textAlign: 'center',
                  margin: 0,
                  marginBottom: isMobile ? '16px' : '24px',
                  letterSpacing: '-0.34px',
                  lineHeight: isMobile ? '20px' : '26px',
                }}>{pageContent.worthSubtext}</p>

                {/* Pay Text with arrow */}
                <div style={{
                  display: 'flex',
                  alignItems: 'flex-start',
                  justifyContent: 'center',
                  gap: isMobile ? '6px' : '8px',
                  marginBottom: isMobile ? '16px' : '24px',
                }}>
                  <p style={{
                    fontFamily: "'Inter', sans-serif",
                    fontSize: isMobile ? '18px' : '22px',
                    fontWeight: 700,
                    color: '#000',
                    textAlign: 'center',
                    margin: 0,
                    letterSpacing: '-0.34px',
                  }}>{pageContent.payText}</p>
                  <img
                    src={arrow2Image}
                    alt=""
                    style={{
                      width: isMobile ? '24px' : '32px',
                      height: 'auto',
                      marginTop: isMobile ? '10px' : '14px',
                    }}
                  />
                </div>

                {/* Blue Pricing Box */}
                <div style={{
                  backgroundColor: '#2558BF',
                  borderRadius: isMobile ? '12px' : '15px',
                  padding: isMobile ? '16px 20px 18px' : '18px 39px 20px',
                  marginBottom: isMobile ? '24px' : '36px',
                }}>
                  {/* Price Row */}
                  <div style={{
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    gap: isMobile ? '8px' : '12px',
                    marginBottom: isMobile ? '10px' : '12px',
                  }}>
                    <span style={{
                      fontFamily: "'Inter', sans-serif",
                      fontSize: isMobile ? '12px' : '14px',
                      fontWeight: 500,
                      color: '#fff',
                      textAlign: 'right',
                      lineHeight: 1.2,
                    }}>Starting<br />From</span>
                    <span style={{
                      fontFamily: "'Inter', sans-serif",
                      fontSize: isMobile ? '44px' : '64px',
                      fontWeight: 600,
                      color: '#fff',
                      lineHeight: isMobile ? '44px' : '64px',
                      letterSpacing: '0.28px',
                    }}>{pageContent.priceValue}</span>
                    <span style={{
                      backgroundColor: '#fff',
                      color: '#000',
                      padding: isMobile ? '2px 7px' : '2px 9px',
                      borderRadius: '300px',
                      fontSize: isMobile ? '11px' : '13px',
                      fontWeight: 500,
                      lineHeight: '17px',
                    }}>{pageContent.priceDiscount}</span>
                  </div>

                  {/* Subtext */}
                  <p style={{
                    fontFamily: "'Inter', sans-serif",
                    fontSize: isMobile ? '13px' : '16px',
                    fontWeight: 500,
                    color: '#ececec',
                    textAlign: 'center',
                    margin: 0,
                    marginBottom: isMobile ? '16px' : '24px',
                    letterSpacing: '-0.16px',
                    lineHeight: isMobile ? '18px' : '21px',
                  }}>{pageContent.priceSubtext}</p>

                  {/* CTA Button */}
                  <Link to="/contact" style={{
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    gap: isMobile ? '6px' : '9px',
                    backgroundColor: '#E1FFA0',
                    color: '#1f1f1f',
                    padding: isMobile ? '12px 20px' : '17px 28px',
                    borderRadius: '300px',
                    border: '2px solid #000',
                    textDecoration: 'none',
                    fontFamily: "'Inter', sans-serif",
                    fontSize: isMobile ? '14px' : '17px',
                    fontWeight: 700,
                    lineHeight: isMobile ? '20px' : '26px',
                  }}>
                    {pageContent.priceCTAText}
                    <svg width={isMobile ? '16' : '21'} height={isMobile ? '16' : '21'} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5">
                      <path d="M5 12h14M12 5l7 7-7 7" />
                    </svg>
                  </Link>
                </div>

                {/* Guarantees */}
                <div style={{ display: 'flex', justifyContent: 'space-between', gap: isMobile ? '8px' : '46px', flexWrap: 'nowrap' }}>
                  {[
                    { icon: '🚀', text: pageContent.guarantee1, sub: pageContent.guarantee1Sub },
                    { icon: '♾️', text: pageContent.guarantee2, sub: pageContent.guarantee2Sub },
                    { icon: '✅', text: pageContent.guarantee3, sub: pageContent.guarantee3Sub },
                  ].filter(g => g.text).map((g, i) => (
                    <div key={i} style={{ textAlign: 'center', flex: 1 }}>
                      <div style={{ fontSize: isMobile ? '20px' : '30px', marginBottom: isMobile ? '4px' : '8px', lineHeight: isMobile ? '24px' : '36px' }}>{g.icon}</div>
                      <div style={{
                        fontFamily: "'Inter', sans-serif",
                        fontSize: isMobile ? '11px' : '16px',
                        fontWeight: 600,
                        color: '#000',
                        lineHeight: isMobile ? '14px' : '24px',
                        letterSpacing: '-0.31px',
                      }}>{g.text}</div>
                      <div style={{
                        fontFamily: "'Inter', sans-serif",
                        fontSize: isMobile ? '10px' : '14px',
                        fontWeight: 500,
                        color: '#3b3b3b',
                        lineHeight: isMobile ? '14px' : '20px',
                        letterSpacing: '-0.15px',
                      }}>{g.sub}</div>
                    </div>
                  ))}
                </div>
              </div>

              {/* Features List */}
              <div style={{ width: isMobile ? '100%' : '477px', flexShrink: 0, order: isMobile ? 1 : 2 }}>
                {[
                  { title: pageContent.feature1, desc: pageContent.feature1Desc },
                  { title: pageContent.feature2, desc: pageContent.feature2Desc },
                  { title: pageContent.feature3, desc: pageContent.feature3Desc },
                  { title: pageContent.feature4, desc: pageContent.feature4Desc },
                  { title: pageContent.feature5, desc: pageContent.feature5Desc },
                  { title: pageContent.feature6, desc: pageContent.feature6Desc },
                ].filter(f => f.title).map((feature, i, arr) => (
                  <div key={i} style={{
                    display: 'flex',
                    alignItems: 'flex-start',
                    gap: isMobile ? '10px' : '13px',
                    padding: isMobile ? '14px 0' : '18px',
                    borderBottom: (isMobile && i === arr.length - 1) ? 'none' : '1px solid #000',
                  }}>
                    {/* Blue Checkmark Circle */}
                    <div style={{
                      width: isMobile ? '22px' : '26px',
                      height: isMobile ? '22px' : '26px',
                      backgroundColor: '#2558BF',
                      borderRadius: '50%',
                      display: 'flex',
                      alignItems: 'center',
                      justifyContent: 'center',
                      flexShrink: 0,
                      marginTop: isMobile ? '2px' : '4px',
                    }}>
                      <svg width={isMobile ? '14' : '17'} height={isMobile ? '14' : '17'} viewBox="0 0 24 24" fill="none" stroke="#fff" strokeWidth="3">
                        <path d="M20 6L9 17l-5-5" />
                      </svg>
                    </div>
                    <div>
                      <h4 style={{
                        fontFamily: "'Inter', sans-serif",
                        fontSize: isMobile ? '15px' : '18px',
                        fontWeight: 600,
                        color: '#000',
                        margin: 0,
                        marginBottom: '2px',
                        lineHeight: isMobile ? '20px' : '26px',
                        letterSpacing: '-0.34px',
                      }}>{feature.title}</h4>
                      <p style={{
                        fontFamily: "'Inter', sans-serif",
                        fontSize: isMobile ? '13px' : '15px',
                        fontWeight: 500,
                        color: '#99a1af',
                        margin: 0,
                        lineHeight: isMobile ? '18px' : '21px',
                        letterSpacing: '-0.16px',
                      }}>{feature.desc}</p>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          </div>
        </section>
      </EditableSection>

      {/* Testimonials Section */}
      <EditableSection sectionId="testimonials" label="Testimonials Section" isEditorMode={isEditorMode} isSelected={selectedSection === 'testimonials'}>
        <section style={{
          padding: isMobile ? '32px 20px' : '60px 63px',
          backgroundColor: '#FFFDF9',
          width: '100%',
          boxSizing: 'border-box',
        }}>
          <div style={{ maxWidth: '1018px', margin: '0 auto' }}>
            {/* Title */}
            <div style={{ textAlign: 'center', marginBottom: isMobile ? '12px' : '16px' }}>
              <h2 style={{
                fontFamily: "'Inter', sans-serif",
                fontSize: isMobile ? '24px' : '48px',
                fontWeight: 700,
                color: '#000',
                margin: 0,
                lineHeight: 1.4,
              }}>
                {pageContent.testimonialTitle}{' '}
                <span style={{
                  backgroundColor: '#E2775A',
                  color: '#fff',
                  padding: isMobile ? '2px 10px' : '4px 16px',
                  fontStyle: 'italic',
                  whiteSpace: 'nowrap',
                }}>Our Word</span>
                <br />
                <span style={{
                  backgroundColor: '#E2775A',
                  color: '#fff',
                  padding: isMobile ? '2px 10px' : '4px 16px',
                  fontStyle: 'italic',
                  whiteSpace: 'nowrap',
                }}>For It</span>
              </h2>
            </div>

            {/* Subtitle */}
            <p style={{
              fontFamily: "'Inter', sans-serif",
              fontSize: isMobile ? '14px' : '16px',
              fontWeight: 400,
              color: '#4a5565',
              textAlign: 'center',
              maxWidth: '700px',
              margin: isMobile ? '0 auto 24px' : '0 auto 40px',
              lineHeight: isMobile ? '20px' : '26px',
            }}>{pageContent.testimonialSubtitle}</p>

            {/* Testimonial Card */}
            {(() => {
              const testimonials = pageContent.testimonials || [];
              const currentTestimonial = testimonials[currentTestimonialIndex] || testimonials[0] || {};
              const hasMultiple = testimonials.length > 1;

              const goToPrev = () => setCurrentTestimonialIndex(prev => Math.max(0, prev - 1));
              const goToNext = () => setCurrentTestimonialIndex(prev => Math.min(testimonials.length - 1, prev + 1));

              return (
                <>
                  <div style={{
                    backgroundColor: '#F8F8F8',
                    borderRadius: '16px',
                    border: '1.5px solid #000',
                    padding: isMobile ? '20px' : '30px 44px 44px 44px',
                    position: 'relative',
                    minHeight: isMobile ? 'auto' : '320px',
                  }}>
                    {/* Navigation arrows on sides - Desktop only */}
                    {!isMobile && hasMultiple && (
                      <>
                        <button
                          onClick={goToPrev}
                          disabled={currentTestimonialIndex === 0}
                          style={{
                            position: 'absolute',
                            left: '-27px',
                            top: '50%',
                            transform: 'translateY(-50%)',
                            width: '54px',
                            height: '54px',
                            borderRadius: '50%',
                            border: '1.5px solid #000',
                            backgroundColor: '#fff',
                            cursor: currentTestimonialIndex === 0 ? 'not-allowed' : 'pointer',
                            display: 'flex',
                            alignItems: 'center',
                            justifyContent: 'center',
                            zIndex: 2,
                            opacity: currentTestimonialIndex === 0 ? 0.5 : 1,
                          }}>
                          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#000" strokeWidth="2">
                            <path d="M19 12H5M12 19l-7-7 7-7" />
                          </svg>
                        </button>
                        <button
                          onClick={goToNext}
                          disabled={currentTestimonialIndex === testimonials.length - 1}
                          style={{
                            position: 'absolute',
                            right: '-27px',
                            top: '50%',
                            transform: 'translateY(-50%)',
                            width: '54px',
                            height: '54px',
                            borderRadius: '50%',
                            border: '1.5px solid #000',
                            backgroundColor: '#fff',
                            cursor: currentTestimonialIndex === testimonials.length - 1 ? 'not-allowed' : 'pointer',
                            display: 'flex',
                            alignItems: 'center',
                            justifyContent: 'center',
                            zIndex: 2,
                            opacity: currentTestimonialIndex === testimonials.length - 1 ? 0.5 : 1,
                          }}>
                          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#000" strokeWidth="2">
                            <path d="M5 12h14M12 5l7 7-7 7" />
                          </svg>
                        </button>
                      </>
                    )}

                    {/* Mobile Layout - Side by side */}
                    {isMobile ? (
                      <>
                        <div style={{
                          display: 'flex',
                          gap: '16px',
                          marginBottom: '20px',
                        }}>
                          {/* Quote Section - Left */}
                          <div style={{ flex: 1 }}>
                            {/* Quote Mark */}
                            <img
                              src={quoteIcon}
                              alt="Quote"
                              style={{
                                width: '28px',
                                height: 'auto',
                                display: 'block',
                                marginBottom: '12px',
                              }}
                            />

                            {/* Quote Text - Paragraph 1 */}
                            <p style={{
                              fontFamily: "'Inter', sans-serif",
                              fontSize: '14px',
                              fontWeight: 400,
                              lineHeight: '22px',
                              color: '#000',
                              margin: 0,
                              marginBottom: '12px',
                            }}>{currentTestimonial.quote1}</p>

                            {/* Quote Text - Paragraph 2 */}
                            <p style={{
                              fontFamily: "'Inter', sans-serif",
                              fontSize: '14px',
                              fontWeight: 400,
                              lineHeight: '22px',
                              color: '#000',
                              margin: 0,
                            }}>{currentTestimonial.quote2}</p>
                          </div>

                          {/* Author Section - Right */}
                          <div style={{ textAlign: 'center', width: '109px', flexShrink: 0 }}>
                            <div style={{
                              width: '109px',
                              height: '109px',
                              borderRadius: '50%',
                              backgroundColor: '#D9D9D9',
                              margin: '0 auto 10px',
                              overflow: 'hidden',
                            }}>
                              {currentTestimonial.authorImage && (
                                <img src={getImageUrl(currentTestimonial.authorImage)} alt={currentTestimonial.authorName} style={{ width: '100%', height: '100%', objectFit: 'cover' }} />
                              )}
                            </div>
                            <h4 style={{
                              fontFamily: "'Inter', sans-serif",
                              fontSize: '16px',
                              fontWeight: 600,
                              color: '#1f1f1f',
                              margin: 0,
                              marginBottom: '4px',
                              letterSpacing: '-0.15px',
                            }}>{currentTestimonial.authorName}</h4>
                            <p style={{
                              fontFamily: "'Inter', sans-serif",
                              fontSize: '12px',
                              fontWeight: 500,
                              color: '#6b6b6b',
                              margin: 0,
                              lineHeight: '20px',
                            }}>({currentTestimonial.authorRole})</p>
                          </div>
                        </div>

                        {/* Stats Row - Mobile */}
                        <div style={{
                          display: 'flex',
                          borderTop: '1px solid #E5E7EB',
                          paddingTop: '16px',
                        }}>
                          {[
                            { value: currentTestimonial.stat1Value, label: currentTestimonial.stat1Label },
                            { value: currentTestimonial.stat2Value, label: currentTestimonial.stat2Label },
                            { value: currentTestimonial.stat3Value, label: currentTestimonial.stat3Label },
                          ].filter(s => s.value).map((stat, i, arr) => (
                            <div key={i} style={{
                              flex: 1,
                              borderRight: i < arr.length - 1 ? '1px solid #E5E7EB' : 'none',
                              paddingRight: i < arr.length - 1 ? '12px' : '0',
                              paddingLeft: i > 0 ? '12px' : '0',
                            }}>
                              <div style={{
                                fontFamily: "'Inter', sans-serif",
                                fontSize: '18px',
                                fontWeight: 700,
                                color: '#000',
                                marginBottom: '2px',
                                lineHeight: '22px',
                              }}>{stat.value}</div>
                              <div style={{
                                fontFamily: "'Inter', sans-serif",
                                fontSize: '11px',
                                fontWeight: 400,
                                color: '#6B7280',
                                lineHeight: '14px',
                              }}>{stat.label}</div>
                            </div>
                          ))}
                        </div>
                      </>
                    ) : (
                      /* Desktop Layout */
                      <div style={{
                        display: 'flex',
                        gap: '40px',
                        alignItems: 'center',
                        paddingLeft: '40px',
                        paddingRight: '40px',
                      }}>
                        {/* Quote Section */}
                        <div style={{ flex: 1 }}>
                          {/* Quote Mark */}
                          <img
                            src={quoteIcon}
                            alt="Quote"
                            style={{
                              width: '36px',
                              height: 'auto',
                              display: 'block',
                              marginBottom: '16px',
                            }}
                          />

                          {/* Quote Text - Paragraph 1 */}
                          <p style={{
                            fontFamily: "'Inter', sans-serif",
                            fontSize: '15px',
                            fontWeight: 400,
                            lineHeight: '24px',
                            color: '#000',
                            margin: 0,
                            marginBottom: '16px',
                          }}>{currentTestimonial.quote1}</p>

                          {/* Quote Text - Paragraph 2 */}
                          <p style={{
                            fontFamily: "'Inter', sans-serif",
                            fontSize: '15px',
                            fontWeight: 400,
                            lineHeight: '24px',
                            color: '#000',
                            margin: 0,
                            marginBottom: '28px',
                          }}>{currentTestimonial.quote2}</p>

                          {/* Stats Row */}
                          <div style={{
                            display: 'flex',
                            gap: '0',
                          }}>
                            {[
                              { value: currentTestimonial.stat1Value, label: currentTestimonial.stat1Label },
                              { value: currentTestimonial.stat2Value, label: currentTestimonial.stat2Label },
                              { value: currentTestimonial.stat3Value, label: currentTestimonial.stat3Label },
                            ].filter(s => s.value).map((stat, i, arr) => (
                              <div key={i} style={{
                                borderRight: i < arr.length - 1 ? '1px solid #E5E7EB' : 'none',
                                paddingRight: i < arr.length - 1 ? '24px' : '0',
                                paddingLeft: i > 0 ? '24px' : '0',
                              }}>
                                <div style={{
                                  fontFamily: "'Inter', sans-serif",
                                  fontSize: '24px',
                                  fontWeight: 600,
                                  color: '#000',
                                  marginBottom: '2px',
                                  lineHeight: '28px',
                                }}>{stat.value}</div>
                                <div style={{
                                  fontFamily: "'Inter', sans-serif",
                                  fontSize: '11px',
                                  fontWeight: 400,
                                  color: '#6B7280',
                                  lineHeight: '14px',
                                }}>{stat.label}</div>
                              </div>
                            ))}
                          </div>
                        </div>

                        {/* Author Section */}
                        <div style={{ textAlign: 'center', minWidth: '255px' }}>
                          <div style={{
                            width: '255px',
                            height: '255px',
                            borderRadius: '50%',
                            backgroundColor: '#D9D9D9',
                            margin: '0 auto 12px',
                            overflow: 'hidden',
                          }}>
                            {currentTestimonial.authorImage && (
                              <img src={getImageUrl(currentTestimonial.authorImage)} alt={currentTestimonial.authorName} style={{ width: '100%', height: '100%', objectFit: 'cover' }} />
                            )}
                          </div>
                          <h4 style={{
                            fontFamily: "'Inter', sans-serif",
                            fontSize: '16px',
                            fontWeight: 600,
                            color: '#000',
                            margin: 0,
                            marginBottom: '2px',
                          }}>{currentTestimonial.authorName}</h4>
                          <p style={{
                            fontFamily: "'Inter', sans-serif",
                            fontSize: '12px',
                            fontWeight: 400,
                            color: '#6B7280',
                            margin: 0,
                          }}>({currentTestimonial.authorRole})</p>
                        </div>
                      </div>
                    )}
                  </div>

                  {/* Mobile Navigation - Outside container */}
                  {isMobile && hasMultiple && (
                    <div style={{ display: 'flex', justifyContent: 'center', gap: '12px', marginTop: '20px' }}>
                      <button
                        onClick={goToPrev}
                        disabled={currentTestimonialIndex === 0}
                        style={{
                          width: '36px',
                          height: '36px',
                          borderRadius: '50%',
                          border: '1.5px solid #000',
                          backgroundColor: '#fff',
                          cursor: currentTestimonialIndex === 0 ? 'not-allowed' : 'pointer',
                          display: 'flex',
                          alignItems: 'center',
                          justifyContent: 'center',
                          opacity: currentTestimonialIndex === 0 ? 0.5 : 1,
                        }}>
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#000" strokeWidth="2">
                          <path d="M19 12H5M12 19l-7-7 7-7" />
                        </svg>
                      </button>
                      <button
                        onClick={goToNext}
                        disabled={currentTestimonialIndex === testimonials.length - 1}
                        style={{
                          width: '36px',
                          height: '36px',
                          borderRadius: '50%',
                          border: '1.5px solid #000',
                          backgroundColor: '#fff',
                          cursor: currentTestimonialIndex === testimonials.length - 1 ? 'not-allowed' : 'pointer',
                          display: 'flex',
                          alignItems: 'center',
                          justifyContent: 'center',
                          opacity: currentTestimonialIndex === testimonials.length - 1 ? 0.5 : 1,
                        }}>
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#000" strokeWidth="2">
                          <path d="M5 12h14M12 5l7 7-7 7" />
                        </svg>
                      </button>
                    </div>
                  )}
                </>
              );
            })()}
          </div>
        </section>
      </EditableSection>

      {/* Social Proof */}
      <EditableSection sectionId="socialProof" label="Social Proof Section" isEditorMode={isEditorMode} isSelected={selectedSection === 'socialProof'}>
        <section style={{
          padding: isMobile ? '20px 20px' : '40px 63px',
          backgroundColor: '#FFFDF9',
          width: '100%',
          boxSizing: 'border-box',
        }}>
          {isMobile ? (
            /* Mobile Layout - Matching Figma exactly */
            /* Everything BETWEEN the two olive branches */
            <div style={{
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              width: '100%',
            }}>
              {/* Left Olive Branch */}
              <img
                src={oliveBranchLeft}
                alt=""
                style={{
                  width: '42px',
                  height: '95.389px',
                  flexShrink: 0,
                }}
              />

              {/* Center Content - All between olive branches */}
              <div style={{
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                flex: 1,
                padding: '0 8px',
              }}>
                {/* 990+ and stars */}
                <div style={{
                  fontFamily: "'Inter', sans-serif",
                  fontSize: '32px',
                  fontWeight: 700,
                  color: '#000',
                  lineHeight: 1,
                  textAlign: 'center',
                }}>{pageContent.reviewCount}</div>
                <div style={{ color: '#000', fontSize: '16px', letterSpacing: '2px', lineHeight: 1, marginBottom: '8px', textAlign: 'center' }}>{'★'.repeat(5)}</div>

                {/* Bottom Row: Text | Avatars | +195 more */}
                <div style={{
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  gap: '6px',
                  width: '100%',
                }}>
                  {/* Text */}
                  <div style={{
                    textAlign: 'left',
                  }}>
                    <p style={{
                      fontFamily: "'Inter', sans-serif",
                      fontSize: '11px',
                      fontWeight: 600,
                      color: '#000',
                      margin: 0,
                      lineHeight: '14px',
                    }}>{pageContent.reviewText}</p>
                    <p style={{
                      fontFamily: "'Inter', sans-serif",
                      fontSize: '10px',
                      fontWeight: 400,
                      color: '#6B7280',
                      margin: 0,
                      lineHeight: '13px',
                    }}>{pageContent.reviewSubtext}</p>
                  </div>

                  {/* Avatars */}
                  <div style={{
                    display: 'flex',
                    alignItems: 'center',
                    flexShrink: 0,
                  }}>
                    {[1,2,3].map(i => (
                      <div key={i} style={{
                        width: '24px',
                        height: '24px',
                        borderRadius: '50%',
                        backgroundColor: '#E5E7EB',
                        border: '2px solid #fff',
                        marginLeft: i > 1 ? '-8px' : 0,
                        overflow: 'hidden',
                      }}>
                        {pageContent[`avatar${i}`] && (
                          <img src={getImageUrl(pageContent[`avatar${i}`])} alt="" style={{ width: '100%', height: '100%', objectFit: 'cover' }} />
                        )}
                      </div>
                    ))}
                  </div>

                  {/* +195 more */}
                  <span style={{
                    fontFamily: "'Inter', sans-serif",
                    fontSize: '11px',
                    fontWeight: 600,
                    color: '#000',
                    whiteSpace: 'nowrap',
                  }}>{pageContent.moreCount}</span>
                </div>
              </div>

              {/* Right Olive Branch */}
              <img
                src={oliveBranchRight}
                alt=""
                style={{
                  width: '42px',
                  height: '95.389px',
                  flexShrink: 0,
                }}
              />
            </div>
          ) : (
            /* Desktop Layout */
            <div style={{
              maxWidth: '780px',
              margin: '0 auto',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              gap: '24px',
            }}>
              {/* Left Olive Branch */}
              <img
                src={oliveBranchLeft}
                alt=""
                style={{
                  width: '42px',
                  height: '95.389px',
                }}
              />

              {/* Rating */}
              <div style={{
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'flex-start',
              }}>
                <div style={{
                  fontFamily: "'Inter', sans-serif",
                  fontSize: '40px',
                  fontWeight: 700,
                  color: '#000',
                  lineHeight: 1,
                }}>{pageContent.reviewCount}</div>
                <div style={{ color: '#000', fontSize: '24.423px', letterSpacing: '2px', lineHeight: 1 }}>{'★'.repeat(5)}</div>
              </div>

              {/* Text */}
              <div style={{
                textAlign: 'left',
              }}>
                <p style={{
                  fontFamily: "'Inter', sans-serif",
                  fontSize: '16px',
                  fontWeight: 600,
                  color: '#000',
                  margin: 0,
                  marginBottom: '4px',
                }}>{pageContent.reviewText}</p>
                <p style={{
                  fontFamily: "'Inter', sans-serif",
                  fontSize: '14px',
                  fontWeight: 400,
                  color: '#6B7280',
                  margin: 0,
                }}>{pageContent.reviewSubtext}</p>
              </div>

              {/* Avatars */}
              <div style={{
                display: 'flex',
                alignItems: 'center',
              }}>
                {[1,2,3].map(i => (
                  <div key={i} style={{
                    width: '36px',
                    height: '36px',
                    borderRadius: '50%',
                    backgroundColor: '#E5E7EB',
                    border: '2px solid #fff',
                    marginLeft: i > 1 ? '-10px' : 0,
                    overflow: 'hidden',
                  }}>
                    {pageContent[`avatar${i}`] && (
                      <img src={getImageUrl(pageContent[`avatar${i}`])} alt="" style={{ width: '100%', height: '100%', objectFit: 'cover' }} />
                    )}
                  </div>
                ))}
                <span style={{
                  marginLeft: '12px',
                  fontFamily: "'Inter', sans-serif",
                  fontSize: '14px',
                  fontWeight: 600,
                  color: '#000',
                }}>{pageContent.moreCount}</span>
              </div>

              {/* Right Olive Branch */}
              <img
                src={oliveBranchRight}
                alt=""
                style={{
                  width: '42px',
                  height: '95.389px',
                }}
              />
            </div>
          )}
        </section>
      </EditableSection>

      {/* FAQ Section */}
      <EditableSection sectionId="faq" label="FAQ Section" isEditorMode={isEditorMode} isSelected={selectedSection === 'faq'}>
        <section style={{
          padding: isMobile ? '32px 20px' : '80px 63px',
          backgroundColor: '#FFFDF9',
          width: '100%',
          boxSizing: 'border-box',
        }}>
          <div style={{ maxWidth: '900px', margin: '0 auto' }}>
            {/* Title */}
            <h2 style={{
              fontFamily: "'Inter', sans-serif",
              fontSize: isMobile ? '24px' : '48px',
              fontWeight: 700,
              textAlign: 'center',
              color: '#000',
              margin: 0,
              marginBottom: isMobile ? '32px' : '56px',
            }}>{pageContent.faqTitle}</h2>

            {/* FAQ Items */}
            <div style={{ display: 'flex', flexDirection: 'column', gap: isMobile ? '12px' : '16px' }}>
              {[
                { q: pageContent.faq1Question, a: pageContent.faq1Answer },
                { q: pageContent.faq2Question, a: pageContent.faq2Answer },
                { q: pageContent.faq3Question, a: pageContent.faq3Answer },
                { q: pageContent.faq4Question, a: pageContent.faq4Answer },
                { q: pageContent.faq5Question, a: pageContent.faq5Answer },
                { q: pageContent.faq6Question, a: pageContent.faq6Answer },
                { q: pageContent.faq7Question, a: pageContent.faq7Answer },
              ].filter(f => f.q).map((faq, i) => (
                <div key={i} style={{
                  display: 'flex',
                  flexDirection: 'column',
                  alignItems: 'flex-start',
                  gap: isMobile ? '8px' : '10px',
                  padding: isMobile ? '16px' : '20px 32px 20px 24px',
                  borderRadius: isMobile ? '10px' : '12px',
                  backgroundColor: '#F9F3E6',
                }}>
                  <button
                    onClick={() => setOpenFaq(openFaq === i ? null : i)}
                    style={{
                      width: '100%',
                      display: 'flex',
                      justifyContent: 'space-between',
                      alignItems: 'center',
                      background: 'none',
                      border: 'none',
                      cursor: 'pointer',
                      padding: 0,
                      textAlign: 'left',
                    }}
                  >
                    <span style={{
                      fontFamily: "'Inter', sans-serif",
                      fontSize: isMobile ? '14px' : '20px',
                      fontWeight: 600,
                      paddingRight: isMobile ? '12px' : '20px',
                      color: '#000',
                      lineHeight: isMobile ? '20px' : '26px',
                      letterSpacing: '0.2px',
                    }}>{faq.q}</span>
                    <span style={{
                      fontFamily: "'Inter', sans-serif",
                      fontSize: isMobile ? '20px' : '24px',
                      fontWeight: 300,
                      color: '#000',
                      flexShrink: 0,
                      transition: 'transform 0.2s ease',
                      transform: openFaq === i ? 'rotate(45deg)' : 'rotate(0deg)',
                    }}>+</span>
                  </button>
                  {openFaq === i && (
                    <p style={{
                      fontFamily: "'Inter', sans-serif",
                      fontSize: isMobile ? '13px' : '15px',
                      color: '#6B7280',
                      lineHeight: isMobile ? 1.6 : 1.7,
                      margin: 0,
                      paddingRight: isMobile ? '24px' : '40px',
                    }}>{faq.a}</p>
                  )}
                </div>
              ))}
            </div>
          </div>
        </section>
      </EditableSection>

      {/* Contact Form Section - Have More Questions */}
      <EditableSection sectionId="contactForm" label="Contact Form Section" isEditorMode={isEditorMode} isSelected={selectedSection === 'contactForm'}>
        <section style={{
          padding: isMobile ? '40px 20px' : '83px 84px 94px 83px',
          backgroundColor: '#FFFDF9',
          width: '100%',
          boxSizing: 'border-box',
        }}>
          <div style={{ maxWidth: '1200px', margin: '0 auto' }}>
            <div style={{
              display: 'flex',
              flexDirection: isMobile ? 'column' : 'row',
              gap: isMobile ? '32px' : '70px',
              alignItems: 'flex-start',
            }}>
              {/* Left - Title */}
              <div style={{ flex: '0 0 auto', minWidth: isMobile ? '100%' : '280px' }}>
                <h2 style={{
                  fontFamily: "'Inter', sans-serif",
                  fontSize: isMobile ? '28px' : '36px',
                  fontWeight: 700,
                  color: '#000',
                  margin: 0,
                  marginBottom: '20px',
                  lineHeight: 1.2,
                  textAlign: isMobile ? 'center' : 'left',
                }}>
                  {pageContent.formTitle || 'Have More Question?'}<br />{pageContent.formSubtitle || 'Book A Call'}
                </h2>
                <div style={{ display: 'flex', gap: isMobile ? '6px' : '8px', flexWrap: 'nowrap', justifyContent: isMobile ? 'center' : 'flex-start' }}>
                  {(formTags.length > 0 ? formTags : ['Branding', 'Packaging', 'Website']).map((tag, i) => (
                    <span key={i} style={{
                      display: 'flex',
                      alignItems: 'center',
                      gap: '8px',
                      backgroundColor: '#2558BF',
                      color: '#fff',
                      padding: isMobile ? '10px 16px' : '12px 24px',
                      borderRadius: '16777200px',
                      fontFamily: "'Inter', sans-serif",
                      fontSize: isMobile ? '12px' : '14px',
                      fontWeight: 500,
                      whiteSpace: 'nowrap',
                    }}>{tag}</span>
                  ))}
                </div>
              </div>

              {/* Right - Form */}
              {formSuccess ? (
                <div style={{
                  flex: 1,
                  display: 'flex',
                  flexDirection: 'column',
                  alignItems: 'center',
                  justifyContent: 'center',
                  padding: '60px 40px',
                  backgroundColor: '#fff',
                  borderRadius: '12px',
                  border: '2px solid #000',
                  textAlign: 'center',
                }}>
                  <div style={{
                    width: '60px',
                    height: '60px',
                    borderRadius: '50%',
                    backgroundColor: '#E1FFA0',
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    marginBottom: '20px',
                  }}>
                    <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="#000" strokeWidth="2">
                      <polyline points="20 6 9 17 4 12" />
                    </svg>
                  </div>
                  <h3 style={{
                    fontFamily: "'Inter', sans-serif",
                    fontSize: '28px',
                    fontWeight: 700,
                    color: '#000',
                    margin: 0,
                    marginBottom: '12px',
                  }}>Thank you!</h3>
                  <p style={{
                    fontFamily: "'Inter', sans-serif",
                    fontSize: '16px',
                    color: '#666',
                    margin: 0,
                    marginBottom: '24px',
                  }}>We've received your message and will get back to you soon.</p>
                  <button
                    onClick={() => setFormSuccess(false)}
                    style={{
                      backgroundColor: '#E1FFA0',
                      color: '#000',
                      padding: '12px 24px',
                      borderRadius: '300px',
                      border: '1px solid #000',
                      fontFamily: "'Inter', sans-serif",
                      fontSize: '16px',
                      fontWeight: 600,
                      cursor: 'pointer',
                    }}
                  >
                    Send another message
                  </button>
                </div>
              ) : (
              <form onSubmit={handleFormSubmit} style={{ flex: 1, width: '100%' }}>
                {formError && (
                  <div style={{
                    backgroundColor: '#FEE2E2',
                    border: '1px solid #FECACA',
                    borderRadius: '8px',
                    padding: '12px 16px',
                    marginBottom: '20px',
                    color: '#DC2626',
                    fontFamily: "'Inter', sans-serif",
                    fontSize: '14px',
                  }}>
                    {formError}
                  </div>
                )}
                <input
                  type="text"
                  placeholder="Full Name*"
                  value={formData.fullName}
                  onChange={(e) => setFormData({...formData, fullName: e.target.value})}
                  required
                  style={{
                    width: '100%',
                    height: isMobile ? '56px' : '80px',
                    padding: isMobile ? '0 16px' : '0 26px',
                    borderRadius: '12px',
                    border: '2px solid #000',
                    marginBottom: isMobile ? '16px' : '24px',
                    fontFamily: "'Inter', sans-serif",
                    fontSize: isMobile ? '16px' : '22px',
                    fontWeight: 500,
                    lineHeight: '140%',
                    color: '#000',
                    boxSizing: 'border-box',
                    backgroundColor: '#fff',
                    outline: 'none',
                  }}
                />
                <input
                  type="email"
                  placeholder="Email ID*"
                  value={formData.email}
                  onChange={(e) => setFormData({...formData, email: e.target.value})}
                  required
                  style={{
                    width: '100%',
                    height: isMobile ? '56px' : '80px',
                    padding: isMobile ? '0 16px' : '0 26px',
                    borderRadius: '12px',
                    border: '2px solid #000',
                    marginBottom: isMobile ? '16px' : '24px',
                    fontFamily: "'Inter', sans-serif",
                    fontSize: isMobile ? '16px' : '22px',
                    fontWeight: 500,
                    lineHeight: '140%',
                    color: '#000',
                    boxSizing: 'border-box',
                    backgroundColor: '#fff',
                    outline: 'none',
                  }}
                />
                <input
                  type="text"
                  placeholder="Company Name*"
                  value={formData.companyName}
                  onChange={(e) => setFormData({...formData, companyName: e.target.value})}
                  required
                  style={{
                    width: '100%',
                    height: isMobile ? '56px' : '80px',
                    padding: isMobile ? '0 16px' : '0 26px',
                    borderRadius: '12px',
                    border: '2px solid #000',
                    marginBottom: isMobile ? '16px' : '24px',
                    fontFamily: "'Inter', sans-serif",
                    fontSize: isMobile ? '16px' : '22px',
                    fontWeight: 500,
                    lineHeight: '140%',
                    color: '#000',
                    boxSizing: 'border-box',
                    backgroundColor: '#fff',
                    outline: 'none',
                  }}
                />
                <input
                  type="tel"
                  placeholder="Phone Number*"
                  value={formData.phone}
                  onChange={(e) => setFormData({...formData, phone: e.target.value})}
                  required
                  style={{
                    width: '100%',
                    height: isMobile ? '56px' : '80px',
                    padding: isMobile ? '0 16px' : '0 30px',
                    borderRadius: '12px',
                    border: '2px solid #000',
                    marginBottom: isMobile ? '16px' : '24px',
                    fontFamily: "'Inter', sans-serif",
                    fontSize: isMobile ? '16px' : '22px',
                    fontWeight: 500,
                    lineHeight: '140%',
                    color: '#000',
                    boxSizing: 'border-box',
                    backgroundColor: '#fff',
                    outline: 'none',
                  }}
                />
                <textarea
                  placeholder="Tell us more about this project"
                  value={formData.message}
                  onChange={(e) => setFormData({...formData, message: e.target.value})}
                  style={{
                    width: '100%',
                    height: isMobile ? '150px' : '227px',
                    padding: isMobile ? '16px' : '26px',
                    borderRadius: '12px',
                    border: '2px solid #000',
                    marginBottom: isMobile ? '24px' : '40px',
                    fontFamily: "'Inter', sans-serif",
                    fontSize: isMobile ? '16px' : '22px',
                    fontWeight: 500,
                    lineHeight: '140%',
                    color: '#000',
                    resize: 'none',
                    boxSizing: 'border-box',
                    backgroundColor: '#fff',
                    outline: 'none',
                  }}
                />
                {/* Button and Response Text Container */}
                <div style={{
                  display: 'flex',
                  flexDirection: 'column',
                  alignItems: 'center',
                  gap: '12px',
                }}>
                  <button
                    type="submit"
                    disabled={formSubmitting}
                    style={{
                      display: 'flex',
                      alignItems: 'center',
                      gap: '6px',
                      backgroundColor: formSubmitting ? '#ccc' : '#E1FFA0',
                      color: '#000',
                      padding: isMobile ? '12px 20px' : '16px 24px',
                      borderRadius: '300px',
                      border: '1px solid #000',
                      fontFamily: "'Inter', sans-serif",
                      fontSize: isMobile ? '18px' : '22px',
                      fontWeight: 700,
                      cursor: formSubmitting ? 'not-allowed' : 'pointer',
                      lineHeight: '22.734px',
                      opacity: formSubmitting ? 0.7 : 1,
                    }}>
                    {formSubmitting ? 'Submitting...' : (pageContent.formButtonText || 'Submit Now')}
                  </button>
                  <div style={{
                    display: 'flex',
                    alignItems: 'center',
                    gap: isMobile ? '8px' : '12px',
                  }}>
                    <svg width={isMobile ? '22' : '28'} height={isMobile ? '22' : '28'} viewBox="0 0 24 24" fill="none" stroke="#000" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                      <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2" />
                    </svg>
                    <span style={{
                      fontFamily: "'Inter', sans-serif",
                      fontSize: isMobile ? '16px' : '20px',
                      fontWeight: 600,
                      fontStyle: 'italic',
                      color: '#000',
                      lineHeight: 1.4,
                    }}>
                      {pageContent.formResponseText || 'Get Instant Response'}
                    </span>
                  </div>
                </div>
              </form>
              )}
            </div>
          </div>
        </section>
      </EditableSection>

      {/* Landing Footer */}
      <EditableSection sectionId="landingFooter" label="Landing Footer" isEditorMode={isEditorMode} isSelected={selectedSection === 'landingFooter'}>
        <footer style={{
          backgroundColor: '#1A1A1A',
          color: '#fff',
          padding: isMobile ? '16px 20px' : '20px 63px',
          textAlign: 'center',
          boxSizing: 'border-box',
        }}>
          <span style={{
            fontFamily: "'Inter', sans-serif",
            fontSize: '14px',
            color: '#9CA3AF',
          }}>
            {pageContent.copyrightText}{' '}
            <Link to="/terms" style={{
              color: '#9CA3AF',
              textDecoration: 'underline',
            }}>{pageContent.siteUseLink}</Link>
          </span>
        </footer>
      </EditableSection>
    </div>
  );
};

export default Landing;
